# Known-good runtime versions from the `touhou_guess` conda env
# Python: 3.12.9
glfw==2.10.0
imgui[glfw]==2.0.0
Pillow==12.1.0
miniaudio==1.61
freetype-py==2.5.1
moderngl==5.12.0

# Keep this pair pinned together. This is the verified Python 3.12 runtime
# used by the current acceptance environment.
numpy==1.26.4
numba==0.60.0
