import{c as t,Q as o,j as d,m as r}from"./chunks/framework.DK3sgdKX.js";const g=JSON.parse('{"title":"PySTG 下一代编辑器未来实施 TODO（固定版）","description":"","frontmatter":{},"headers":[],"relativePath":"EDITOR_IMPLEMENTATION_TODO.md","filePath":"EDITOR_IMPLEMENTATION_TODO.md","lastUpdated":1787235170000}'),s={name:"EDITOR_IMPLEMENTATION_TODO.md"};function c(a,e,n,i,p,l){return o(),d("div",null,[...e[0]||(e[0]=[r(`<h1 id="pystg-下一代编辑器未来实施-todo-固定版" tabindex="-1">PySTG 下一代编辑器未来实施 TODO（固定版） <a class="header-anchor" href="#pystg-下一代编辑器未来实施-todo-固定版" aria-label="Permalink to &quot;PySTG 下一代编辑器未来实施 TODO（固定版）&quot;">​</a></h1><blockquote><p>状态：Active；N7 已暂停，下一任务是 ER2。本文是仓库唯一的未来实施清单，也是 Agent 的交接协议。 产品依据：<a href="./EDITOR_PRODUCT_VISION">EDITOR_PRODUCT_VISION.md</a>；工程边界：<a href="./EDITOR_ARCHITECTURE">EDITOR_ARCHITECTURE.md</a>。 本版固定日期：2026-08-20。历史路线图和完成日志只通过 Git 追溯，不再作为任务来源。</p></blockquote><p>这份文件保留已完成阶段的冻结证据，并详细描述尚未完成的工作。任务 ID 一旦发布就保持稳定；Agent 只能领取依赖链上最早的未完成任务。没有对应测试和证据，不能把“已有一部分代码”当作完成。</p><h2 id="_0-先读这几条固定结论" tabindex="-1">0. 先读这几条固定结论 <a class="header-anchor" href="#_0-先读这几条固定结论" aria-label="Permalink to &quot;0. 先读这几条固定结论&quot;">​</a></h2><h3 id="_0-1-产品边界" tabindex="-1">0.1 产品边界 <a class="header-anchor" href="#_0-1-产品边界" aria-label="Permalink to &quot;0.1 产品边界&quot;">​</a></h3><ul><li>短期和中期不做自然语言或 Agentic 弹幕生成。帧、角度、速度、数量、随机种子、生命周期和预算必须由确定性的属性、曲线、预设、上下文搜索或行为图表达。</li><li>三条职责轴互相解耦：Content 是版本化数据和标准组件，Behavior 是动作/事件/脚本的生命周期，Tool 是编辑器插件。三者共享类型、资源引用、owner/cancel 和调试协议，但不互持 Qt 对象、运行时对象或 Clip ID。</li><li>三层权限不是三套对象模型：Safe API 面向内容作者，Runtime API 面向玩法开发者，Engine API 面向引擎开发者。普通项目不能因为使用一个 Runtime 行为而获得 Engine 内部对象。</li><li>作者文档是唯一真源。运行时变量、事件、实例、trace 和 overlay 不写回文档，不制造 dirty；所有作者编辑都经过 <code>CommandStack</code> 并支持 Undo/Redo。</li></ul><h3 id="_0-2-状态图、时间线、行为图的边界" tabindex="-1">0.2 状态图、时间线、行为图的边界 <a class="header-anchor" href="#_0-2-状态图、时间线、行为图的边界" aria-label="Permalink to &quot;0.2 状态图、时间线、行为图的边界&quot;">​</a></h3><table tabindex="0"><thead><tr><th>视图</th><th>只回答的问题</th><th>允许保存的内容</th><th>不允许承担的内容</th></tr></thead><tbody><tr><td>State Graph</td><td>现在处于哪个阶段，何时转移</td><td>State、转移条件、进入/退出 owner</td><td>逐弹算法、局部采样、隐式长连线</td></tr><tr><td>Timeline</td><td>哪些生命周期在何时有效、持续、停止</td><td>轨道、片段、窗口、激活规则、资源引用</td><td>复制行为图内部节点、直接调用另一个视图的对象</td></tr><tr><td>Behavior Graph/Runtime Behavior</td><td>一个行为内部怎样计算、采样、循环和发布事实</td><td>局部动作、变量读写、事件输出、取消收尾</td><td>持有 State/Timeline 内部 ID、偷偷切状态或背景</td></tr></tbody></table><p>行为、碰撞和对象生命周期只发布“发生了什么”的类型化事实，例如 <code>enemy.hit</code>、<code>bullet.terminated</code>、<code>encounter.cleared</code>。<code>ReactiveClip</code>/<code>ActivationRule</code> 决定事实在当前 State/时间窗口中是否有效，<code>TaskScope</code> 负责启动后的等待、循环、取消和收尾。事件不能携带 <code>start_clip</code>、<code>set_background_scene</code> 等消费者命令。</p><h3 id="_0-3-必须贯穿所有阶段的三个例子" tabindex="-1">0.3 必须贯穿所有阶段的三个例子 <a class="header-anchor" href="#_0-3-必须贯穿所有阶段的三个例子" aria-label="Permalink to &quot;0.3 必须贯穿所有阶段的三个例子&quot;">​</a></h3><ol><li><strong>死亡开花</strong>：子弹自然到期发布 <code>bullet.terminated(reason=expired)</code>；反应可以来自子弹预设，也可以来自后半段时间线的 <code>ReactiveClip</code>。清场、越界、热重载等原因不能误触发自然死亡开花。</li><li><strong>假 Boss 受击反击</strong>：假 Boss 只发布 <code>enemy.hit(target_tag=fake)</code>；关底 State 的 <code>ReactiveClip</code> 过滤事实，选择 <code>count_per_frame</code>、冷却、最大实例和 owner。State 退出优先取消旧反应。</li><li><strong>击破后背景切换</strong>：如果击破代表阶段变化，State Graph 处理转移；如果只是演出，时间线上的反应槽处理 Background cue。Boss/行为不持有背景片段 ID。</li></ol><h3 id="_0-4-顶层作者的默认流程" tabindex="-1">0.4 顶层作者的默认流程 <a class="header-anchor" href="#_0-4-顶层作者的默认流程" aria-label="Permalink to &quot;0.4 顶层作者的默认流程&quot;">​</a></h3><ol><li>项目向导创建道中或关底 Stage 骨架。</li><li>State Graph 建立 Intro/Normal/Enrage/End 等阶段，只在阶段边界放转移。</li><li>每个 State 的时间线安排敌人、Boss 移动、背景、音频、弹幕和演出；有条件的内容放 <code>ReactiveClip</code> 槽位。</li><li>Pattern 先选预设，在 Inspector 中精确调数量、间隔、角度、速度、曲线和预算；只有局部表达力不足时才展开。</li><li>行为图只承接采样、循环、数学和事件生产，不承接整关卡流程。</li><li>预设、Action、受限表达式和行为图都不足以表达新算法时，玩法开发者才写 Runtime Behavior；普通内容作者不需要写脚本。</li><li>预览、调试、seek、reset、保存、重开和热重载都走正式 runtime；运行时 overlay 只读显示。</li></ol><p>时间轴拖动“从头重放到目标帧”的结构性成本本路线不解决，只冻结 reset/replay 的正确性和可观察性。</p><h2 id="_1-已冻结的仓库基线" tabindex="-1">1. 已冻结的仓库基线 <a class="header-anchor" href="#_1-已冻结的仓库基线" aria-label="Permalink to &quot;1. 已冻结的仓库基线&quot;">​</a></h2><p>这些能力已经在 <code>main</code> 上完成，后续任务只能依赖它们，不得重新发明第二套语义：</p><table tabindex="0"><thead><tr><th>基线</th><th>冻结内容</th><th>主要入口</th><th>回归入口</th></tr></thead><tbody><tr><td>N0/N1</td><td>版本化作者资源、<code>SceneDocument</code>、内嵌 State Graph、稳定 UUID、正式 Pattern/Stage 预览</td><td><code>src/editor/document.py</code>、<code>src/editor/stage_compile.py</code></td><td><code>tests/test_scene_v4_contract.py</code>、<code>tests/test_state_graph_document.py</code>、<code>tests/test_state_graph_runtime.py</code></td></tr><tr><td>N2</td><td>类型化变量、六类作用域、单一写入者、reducer、只读 Engine Snapshot、CommandStack、reset/seek/hot-reload</td><td><code>src/authoring/variables.py</code>、<code>src/game/stage/program.py</code></td><td><code>tests/test_typed_variables.py</code>、<code>tests/test_variable_runtime.py</code>、<code>tests/test_variable_scopes_and_reducers.py</code>、<code>tests/test_variable_seek.py</code>、<code>tests/test_variable_hotreload.py</code></td></tr><tr><td>N3</td><td>本帧 Outbox→下一帧 Inbox、类型化生命周期事实、批量生命周期事件、<code>ReactionSpec</code>、<code>TaskScope</code>、State/Background hook</td><td><code>src/game/events.py</code>、<code>src/game/reactions.py</code>、<code>src/game/stage/context.py</code></td><td><code>tests/test_lifecycle_events.py</code>、<code>tests/test_frame_boundary_events.py</code>、<code>tests/test_lifecycle_batching.py</code>、<code>tests/test_reactions.py</code>、<code>tests/test_reaction_scheduler.py</code>、<code>tests/test_lifecycle_timeline_hooks.py</code>、<code>tests/test_background_reactions.py</code></td></tr></tbody></table><p>N2/N3 focused gate 和全量回归必须保持绿色。它们不是后续阶段可以删掉的“旧测试”；若契约必须改变，先提交独立的 contract-revision 并停止后续阶段。</p><h2 id="_2-每个-agent-必须遵循的执行协议" tabindex="-1">2. 每个 Agent 必须遵循的执行协议 <a class="header-anchor" href="#_2-每个-agent-必须遵循的执行协议" aria-label="Permalink to &quot;2. 每个 Agent 必须遵循的执行协议&quot;">​</a></h2><p>每个任务都按 <code>Read → Audit → Contract → Implement → Verify → Record</code> 执行，且交接卡必须写明任务边界。</p><h3 id="_2-1-read" tabindex="-1">2.1 Read <a class="header-anchor" href="#_2-1-read" aria-label="Permalink to &quot;2.1 Read&quot;">​</a></h3><ol><li>运行 <code>git status --short</code>，记录并保护工作树中不属于本任务的改动。</li><li>完整阅读任务列出的产品章节、源码入口、schema、fixture 和已有回归；先看真实代码，再看 TODO 的假设。</li><li>确认正式运行入口（compiler、runner、preview、editor command），不得把独立模拟器当成实现目标。</li></ol><h3 id="_2-2-audit" tabindex="-1">2.2 Audit <a class="header-anchor" href="#_2-2-audit" aria-label="Permalink to &quot;2.2 Audit&quot;">​</a></h3><ol><li>运行该任务依赖的 focused gate，记录当前通过数和失败边界。</li><li>用真实 <code>SceneDocument</code>/资源 fixture 观察序列化、编译、运行、预览和 reset 行为。</li><li>明确本任务不处理的相邻文件、UI 面和 API，避免“顺便重构”。</li></ol><h3 id="_2-3-contract" tabindex="-1">2.3 Contract <a class="header-anchor" href="#_2-3-contract" aria-label="Permalink to &quot;2.3 Contract&quot;">​</a></h3><ol><li>只在任务开始时创建列出的 Contract 测试，先形成真实红色基线。</li><li>测试必须有可观察的行为、最小真实 fixture、错误路径和稳定诊断位置；禁止只测 import、源码字符串、空 <code>pass</code>、宽泛 <code>except</code>、<code>skip</code> 或 <code>xfail</code>。</li><li>Contract 一旦提交，后续实现不得削弱断言、吞异常、改成第二套预览或把运行时状态写回文档。</li></ol><h3 id="_2-4-implement" tabindex="-1">2.4 Implement <a class="header-anchor" href="#_2-4-implement" aria-label="Permalink to &quot;2.4 Implement&quot;">​</a></h3><ol><li>只修改任务边界内的 authoring/compiler/runtime/editor 文件。</li><li>保持项目相对资源引用、<code>ProjectContext</code>、批量弹幕路径、owner/cancel 传播和正式 runtime 预览。</li><li>所有文档修改通过 <code>CommandStack</code>；运行时 overlay、trace、预算统计不制造 dirty。</li></ol><h3 id="_2-5-verify" tabindex="-1">2.5 Verify <a class="header-anchor" href="#_2-5-verify" aria-label="Permalink to &quot;2.5 Verify&quot;">​</a></h3><p>按任务顺序运行 focused gate、N2/N3 回归、全量 suite、<code>compileall</code>、资源校验和 <code>git diff --check</code>。证据必须分开标注：Structural、Runtime、Performance、Native visual、Usability。offscreen Qt 或 <code>--help</code> 不能替代原生窗口、性能或人工可用性证据。</p><h3 id="_2-6-record-与停止条件" tabindex="-1">2.6 Record 与停止条件 <a class="header-anchor" href="#_2-6-record-与停止条件" aria-label="Permalink to &quot;2.6 Record 与停止条件&quot;">​</a></h3><p>交接消息或提交说明必须包含：</p><table tabindex="0"><thead><tr><th>字段</th><th>必须写明</th></tr></thead><tbody><tr><td>Task ID / 边界</td><td>领取的 ID，以及明确不处理的相邻任务</td></tr><tr><td>Read / Audit</td><td>实际读过的章节、入口、schema/fixture、回归和 <code>git status</code></td></tr><tr><td>Contract</td><td>新增/扩展的测试、红色基线、真实 fixture 和错误边界</td></tr><tr><td>Implementation</td><td>修改的 authoring/compiler/runtime/editor 文件，以及 owner、取消、迁移和错误路径</td></tr><tr><td>Verification</td><td>focused、回归、全量、compileall、资产校验、diff-check；额外 native/performance/usability 命令</td></tr><tr><td>Evidence / Blocker</td><td>通过数量、环境、报告路径和仍缺的门禁；阻塞时保持 <code>[ ]</code></td></tr></tbody></table><p>遇到以下情况必须停止扩大范围并请求维护者决定：产品契约冲突、schema 无法无损迁移、必须逐弹回调/第二套预览/静默 fallback/绕过 Undo、focused gate 仍红却要进入下一阶段、当前环境无法提供 native/performance/usability 证据，或无法安全合并用户改动。</p><h3 id="_2-7-agent-所有权与独立验收" tabindex="-1">2.7 Agent 所有权与独立验收 <a class="header-anchor" href="#_2-7-agent-所有权与独立验收" aria-label="Permalink to &quot;2.7 Agent 所有权与独立验收&quot;">​</a></h3><ol><li>每个 ER 任务开始前必须写明实现 Agent、验证 Agent、允许路径和禁止路径；默认边界见仓库根 <code>AGENTS.md</code>。</li><li>同一 Agent 不能既实现又最终验收同一 ER 任务。验证 Agent 对产品代码只读，只能运行门禁和更新当前任务的 Evidence/Blocker。</li><li>只有主协调 Agent 可以解决跨目录集成、更新总表状态和宣布进入下一任务；实现 Agent 不得自行勾选完成。</li><li>两个 Agent 不得并发编辑同一模块、兼容出口或 Contract。需要跨边界时先停止，由主协调 Agent 明确扩展 allowlist。</li><li>Contract 修订与产品实现分开审查。实现 Agent 不能删测试、放宽断言、加 <code>skip</code>/<code>xfail</code>、伪造 native/usability 报告或用直接调用槽函数代替真实交互。</li></ol><h2 id="_3-测试与文档清理规则" tabindex="-1">3. 测试与文档清理规则 <a class="header-anchor" href="#_3-测试与文档清理规则" aria-label="Permalink to &quot;3. 测试与文档清理规则&quot;">​</a></h2><h3 id="_3-1-本轮已清理" tabindex="-1">3.1 本轮已清理 <a class="header-anchor" href="#_3-1-本轮已清理" aria-label="Permalink to &quot;3.1 本轮已清理&quot;">​</a></h3><ul><li>历史路线图 <code>docs/EDITOR_ROADMAP_TODO.md</code> 和 <code>docs/ARCHITECTURE_EVALUATION_AND_ROADMAP.md</code> 已从当前 Git 历史的工作协议中移除；本文件是唯一 Todo，不创建 <code>*_TODO_v2.md</code>。</li><li>N4.0 已将 <code>tests/test_activation_rules.py</code>、<code>tests/test_reactive_timeline.py</code>、<code>tests/test_timeline_instance_trace.py</code> 固定为正式 Contract；它们不再是临时草稿，后续阶段必须保持其断言语义。</li><li>删除重复的 <code>tests/test_stage_context_bullet_spawn.py</code>。它只做 regular/polar spawn 的 smoke 检查；极坐标语义由 <code>tests/test_polar_motion_unit.py</code> 覆盖，正式 Pattern/Stage 路径由 <code>tests/test_pattern_runtime.py</code>、<code>tests/test_stage_program.py</code> 和 <code>tests/test_preview_controller.py</code> 覆盖。</li></ul><h3 id="_3-2-保留规则" tabindex="-1">3.2 保留规则 <a class="header-anchor" href="#_3-2-保留规则" aria-label="Permalink to &quot;3.2 保留规则&quot;">​</a></h3><p><code>test_editor_app_smoke.py</code>、<code>test_editor_m3_integration.py</code>、<code>test_editor_m4_integration.py</code>、<code>test_editor_m5_integration.py</code>、<code>test_editor_m6_integration.py</code>、<code>test_editor_m6_workspace.py</code> 以及 <code>test_editor_authoring_integration.py</code>、<code>test_editor_regression_contracts.py</code> 都有保存/编译/运行/Undo/Redo/原生崩溃或插件行为断言，必须保留。不能因为测试慢、需要 Qt、文件名带 smoke、暂时失败或想让 suite 变绿而删除测试。</p><p>以后删除测试必须同时满足：它只验证已经移除的接口或重复交接流程；对应行为已有更窄、更真实的替代测试；提交说明给出替代文件和 suite 数量变化。</p><h3 id="_3-3-共同命令" tabindex="-1">3.3 共同命令 <a class="header-anchor" href="#_3-3-共同命令" aria-label="Permalink to &quot;3.3 共同命令&quot;">​</a></h3><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">env:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">QT_QPA_PLATFORM </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;offscreen&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">python </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">m pytest </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">q</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">python </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">m compileall </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">q main.py src game_content tools tests</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">python tools</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">validate_assets.py </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">format json</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">git diff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">check</span></span></code></pre></div><h3 id="_3-4-2026-08-20-跨阶段工程整改" tabindex="-1">3.4 2026-08-20 跨阶段工程整改 <a class="header-anchor" href="#_3-4-2026-08-20-跨阶段工程整改" aria-label="Permalink to &quot;3.4 2026-08-20 跨阶段工程整改&quot;">​</a></h3><p>对 <code>src/editor/</code> 57 文件做全量 AST 结构比对与调用链可达性追踪后，确认 9 项「已记 <code>[x]</code> 但证据或作者入口不成立」的问题和 7 项冗余。以下改动一律不新增功能、不删测试、不放宽断言。</p><p><strong>门禁与安全网</strong></p><ul><li>pytest 会话绑定改为 PySide6（<code>tests/conftest.py</code> 与 <code>src/qt_compat/__init__.py</code>），与 <code>tools/scene_editor.py</code> 及三个 native gate 使用同一绑定。此前全部测试跑在 PyQt5(sip) 而生产跑 PySide6(shiboken)，两者 C++ 对象所有权与 GC 时机不同，「拖线过程中重建不崩溃」这类测试在 PyQt5 上通过并不构成 PySide6 上的证据。</li><li><code>tools/verify_native_editor_n4.py</code> 重写为真跑 <code>compile_stage → StageRunner</code>，见 N4.2 的 Evidence（2026-08-20 重做）。</li><li><code>tests/test_editor_usability.py::test_n6_usability_claim_and_report_must_agree</code> 取代原先断言「<code>reports/n6_usability.json</code> 不存在」的写法。原断言把报告缺席编码成永久不变量，真正提交人工研究报告的当天测试就会因为错误的理由变红；现在断言的是声明与证据必须一致，两种世界下都成立。</li><li>新增 <code>tests/test_editor_i18n_coverage.py</code>：遍历真实编辑器控件树，对 i18n 层声称拥有却译不出的作者可见文案直接失败。此前 458 条以英文源串为键的词条只有点检测试，改动任一英文文案都会让对应中文静默失效且无人发现。</li></ul><p><strong>作者入口补齐（此前属于「写了实现但没接按钮」）</strong></p><ul><li>Reactive 片段双击进入局部视图：<code>TimelineEditor._clip_activated</code> → <code>navigate_reactive_clip()</code>，该方法此前全仓零调用者。</li><li><code>SetPresetSlotOverrideCommand</code> 与 <code>ApplyPresetMigrationCommand</code> 接入 PatternWorkspace 的插槽表单和迁移按钮，见 N5.3、N5.4 的整改证据。</li><li><code>stage_compile</code> 的 State 动作路径不再静默 <code>continue</code> 跳过错误的变量载荷，与 Clip 路径一样发 <code>invalid_variable_automation</code> 诊断。</li></ul><p><strong>冗余消除（零行为变化的提取，除一处已标注的收敛）</strong></p><ul><li><code>MergeableCommand</code>（<code>src/editor/commands.py</code>）承载原先 12 处逐字重复的 <code>merge_with()</code>；子类只声明 <code>merge_owner</code> / <code>merge_identity</code> / <code>merge_same_keys</code> / <code>merge_values</code>。</li><li><code>SpaceTapSearchMixin</code>（<code>src/editor/action_search.py</code>）承载 Scene、Graph、Timeline 三份重复的「按住空格平移、轻按空格搜索」状态机。提取过程中修掉一个真实缺陷：<code>SceneViewport</code> 在 <code>setDragMode(RubberBandDrag)</code> 之前就记下待恢复的拖动模式，一次没有配对按下的空格释放会静默关掉框选。</li><li>新增 <code>src/editor/pattern_resolve.py</code>，承载 Stage 与 Spell 两条编译路径共用的 Pattern 引用解析与出生点规则。此前 <code>scene_compile</code> 只认直接父级恰为 <code>Emitter</code>，<code>stage_compile</code> 走完整祖先链并接受任何带 <code>x</code>/<code>y</code> 的节点（例如 <code>Boss</code>），同一场景在两条路径下会在不同位置出弹。<strong>行为变化</strong>：<code>Spell &gt; Boss(x,y) &gt; PatternInstance</code> 这类结构的 M3 预览现在采用 Boss 位置，与正式路径一致。<code>scene_compile.py</code> 保留而非删除——<code>tests/test_editor_scene_compile.py</code>、<code>tests/test_editor_m3_integration.py</code> 覆盖的是 M3 无脚本 Spell 流程，删文件等于删一个受测功能；它现在与正式路径共用同一套解析规则，不再是第二套语义。</li><li><code>stage_compile._variable_spec_for</code> 改为委托 <code>_variable_candidates</code>，此前两函数逐行同构。</li><li>删除死符号 <code>flat_snapshot</code>、<code>set_error_visual</code>、<code>set_metadata</code>、<code>template_for_emission</code>，以及仅为兼容测试而隐藏保留的 <code>mode_switch</code> 控件（Qt 虚函数覆写如 <code>mimeTypes</code> / <code>filterAcceptsRow</code> / <code>drawForeground</code> 由框架调用，不动）。</li><li>五个 93–183 语句的 <code>__init__</code>（<code>PatternWorkspace</code>、<code>VariableEditor</code>、<code>StateGraphEditor</code>、<code>PatternPreviewPanel</code>、<code>TimelineEditor</code>）拆为 <code>_build_*</code> 私有构建器，抽出的构建器最大 49 语句（<code>EditorMainWindow._build_ui</code> 的 195 语句属于下面延后的 R1 范围）；<code>PatternWorkspace</code> 六处重复的视图切换收敛为 <code>_show_view()</code>。</li></ul><p><strong>当时明确延后、现已被新决策取代</strong>：该轮没有整改 <code>EditorMainWindow</code> 的状态所有权、协调器、预览和插件边界。后续提交 <code>bdd974e</code> 将窗口按八个 <code>SlotsMixin</code> 拆为多个文件，但 Mixin 仍共享同一窗口实例、私有方法、<code>editor_context</code> 和全局 <code>_refresh()</code>；它只完成物理拆分，不构成架构门禁通过。</p><p><strong>整改门禁（2026-08-20，Python 3.12.7 / PySide6 6.8.1.1）</strong></p><ul><li><code>QT_QPA_PLATFORM=offscreen python -m pytest tests --no-header</code> → <code>641 passed in 539.41s</code>，无 skip/xfail；其中 <code>test_editor_i18n_coverage.py</code> 为本次新增，<code>test_n6_usability_claim_and_report_must_agree</code> 取代同名位置上的旧断言，其余用例集未删减。</li><li><code>python -m compileall -q src tools tests</code> → 通过。</li><li><code>python tools/validate_assets.py --format json</code> → <code>issues</code> 为空。</li><li><code>git diff --check</code> → 无空白错误；改动文件全部保持 LF 行尾（仓库 <code>core.autocrlf=false</code>）。</li></ul><h3 id="_3-5-2026-08-20-架构复审与暂停决定" tabindex="-1">3.5 2026-08-20 架构复审与暂停决定 <a class="header-anchor" href="#_3-5-2026-08-20-架构复审与暂停决定" aria-label="Permalink to &quot;3.5 2026-08-20 架构复审与暂停决定&quot;">​</a></h3><p>对 <code>bdd974e</code> 的只读复审确认：<code>src/editor/</code> 当前 51 个 Python 文件、约 21,157 行；<code>EditorMainWindow</code> 仍是 <code>QMainWindow + 8 SlotsMixin</code>，<code>app.py</code> 有 198 个不同 <code>self</code> 成员，九个窗口/Mixin 模块共享 <code>session</code>，约 69 处调用 <code>_refresh()</code>。<code>ManagedDocument.editor_context</code> 仍是 <code>dict[str, Any]</code>；ER1 Contract 的逐键审计把旧计数 21 更正为 22 个隐式键（18 个文档选择/视图值和 4 个 preview overlay 值）。</p><p>同时确认：</p><ul><li><code>src.authoring.registry</code> 和 <code>src.preview.controller</code> 反向导入 <code>src.editor</code> 文档/编译模块；</li><li><code>graph_workspace</code> 与 <code>pattern_workspace</code> 循环导入；</li><li>窗口同时持有正式 <code>PatternPreviewProcess</code> 与旧裸 <code>QProcess</code> 预览路径；</li><li>窗口同时持有 workbench 与 SDK 两套插件 registry；</li><li><code>SceneEditorSession</code> 与 <code>DocumentManager</code> 重复文档生命周期；</li><li><code>ManagedDocument</code> 的类型标注未覆盖实际支持的 UI/Background；</li><li><code>CURRENT_SCHEMA_VERSION</code> 通过重载相等比较同时与 3 和 4 相等。</li></ul><p>因此维护者决定在 N7 前插入 ER0–ER8。ER 是保持作者语义的架构整改，不是 N7 功能开发，也不重做时间线、行为图、schema、渲染器或正式 runtime。ER8 通过前不得再次执行 N6.4 五人研究；N6.4 通过前不得启动 N7。</p><h2 id="_4-未完成任务总表" tabindex="-1">4. 未完成任务总表 <a class="header-anchor" href="#_4-未完成任务总表" aria-label="Permalink to &quot;4. 未完成任务总表&quot;">​</a></h2><p>依赖顺序固定为：<code>N4 → N5 → N6.3 → ER0 → ER1 → ER2 → ER3 → ER4 → ER5 → ER6 → ER7 → ER8 → N6.4 → N7 → N8 → N9</code>。同一阶段内按编号顺序领取；当前阶段 focused gate 未通过，不得开始下一阶段。</p><table tabindex="0"><thead><tr><th>ID</th><th>主题</th><th>状态</th><th>依赖</th></tr></thead><tbody><tr><td>N4.0</td><td>响应式时间线 Contract</td><td><code>[x]</code></td><td>N3</td></tr><tr><td>N4.1</td><td>ReactiveClip 正式运行时与实例 trace</td><td><code>[x]</code></td><td>N4.0</td></tr><tr><td>N4.2</td><td>时间线槽位、overlay、导航与冲突编辑</td><td><code>[x]</code></td><td>N4.1</td></tr><tr><td>N5.0</td><td>版本化预设 Contract</td><td><code>[x]</code></td><td>N4</td></tr><tr><td>N5.1</td><td>首发预设库</td><td><code>[x]</code></td><td>N5.0</td></tr><tr><td>N5.2</td><td>虚拟展开</td><td><code>[x]</code></td><td>N5.1</td></tr><tr><td>N5.3</td><td>参数覆盖与本地物化</td><td><code>[x]</code></td><td>N5.2</td></tr><tr><td>N5.4</td><td>精确版本迁移</td><td><code>[x]</code></td><td>N5.3</td></tr><tr><td>N6.0</td><td>Action Catalog 与新手流程 Contract</td><td><code>[x]</code></td><td>N5</td></tr><tr><td>N6.1</td><td>上下文感知搜索</td><td><code>[x]</code></td><td>N6.0</td></tr><tr><td>N6.2</td><td>道中/关底骨架模板</td><td><code>[x]</code></td><td>N6.1</td></tr><tr><td>N6.3</td><td>分层展开与连续编辑</td><td><code>[x]</code></td><td>N6.2</td></tr><tr><td>ER0</td><td>文档、边界 Contract 与真实基线</td><td><code>[x]</code></td><td>N6.3</td></tr><tr><td>ER1</td><td>有类型的编辑状态</td><td><code>[x]</code></td><td>ER0</td></tr><tr><td>ER2</td><td>Coordinator 与局部失效</td><td><code>[ ]</code></td><td>ER1</td></tr><tr><td>ER3</td><td>Authoring/Command/Compiler 包边界</td><td><code>[ ]</code></td><td>ER2</td></tr><tr><td>ER4</td><td>唯一 PreviewSession</td><td><code>[ ]</code></td><td>ER3</td></tr><tr><td>ER5</td><td>唯一插件贡献入口</td><td><code>[ ]</code></td><td>ER4</td></tr><tr><td>ER6</td><td>Panel 边界与图形循环</td><td><code>[ ]</code></td><td>ER5</td></tr><tr><td>ER7</td><td>兼容层、schema 与重复生命周期清理</td><td><code>[ ]</code></td><td>ER6</td></tr><tr><td>ER8</td><td>架构整体验收</td><td><code>[ ]</code></td><td>ER7</td></tr><tr><td>N6.4</td><td>Usability gate</td><td><code>[ ]</code></td><td>ER8</td></tr><tr><td>N7.0</td><td>Behavior Descriptor/权限 Contract</td><td><code>[ ]</code></td><td>N6.4</td></tr><tr><td>N7.1</td><td>Safe API</td><td><code>[ ]</code></td><td>N7.0</td></tr><tr><td>N7.2</td><td>Runtime API</td><td><code>[ ]</code></td><td>N7.1</td></tr><tr><td>N7.3</td><td>Engine API</td><td><code>[ ]</code></td><td>N7.2</td></tr><tr><td>N7.4</td><td>分区插件包、依赖锁与回滚</td><td><code>[ ]</code></td><td>N7.3</td></tr><tr><td>N7.5</td><td>ComplexMapEmitter 示范插件</td><td><code>[ ]</code></td><td>N7.4</td></tr><tr><td>N8.0</td><td>Render Graph Contract</td><td><code>[ ]</code></td><td>N7</td></tr><tr><td>N8.1</td><td>PassDescriptor 与编译器</td><td><code>[ ]</code></td><td>N8.0</td></tr><tr><td>N8.2</td><td>受限 PassContext 与资源 lease</td><td><code>[ ]</code></td><td>N8.1</td></tr><tr><td>N8.3</td><td>ModernGL backend adapter</td><td><code>[ ]</code></td><td>N8.2</td></tr><tr><td>N8.4</td><td>Tool/Runtime parity</td><td><code>[ ]</code></td><td>N8.3</td></tr><tr><td>N9.0</td><td>调试、重放、性能 Contract</td><td><code>[ ]</code></td><td>N8</td></tr><tr><td>N9.1</td><td>统一 trace 与 why-not 调试器</td><td><code>[ ]</code></td><td>N9.0</td></tr><tr><td>N9.2</td><td>确定性 replay/seek</td><td><code>[ ]</code></td><td>N9.1</td></tr><tr><td>N9.3</td><td>性能 profile 与预算</td><td><code>[ ]</code></td><td>N9.2</td></tr><tr><td>N9.4</td><td>最终作者工作流</td><td><code>[ ]</code></td><td>N9.3</td></tr><tr><td>N9.5</td><td>发布门禁</td><td><code>[ ]</code></td><td>N9.4</td></tr></tbody></table><h2 id="_5-n4-响应式时间线与蓝图边界" tabindex="-1">5. N4：响应式时间线与蓝图边界 <a class="header-anchor" href="#_5-n4-响应式时间线与蓝图边界" aria-label="Permalink to &quot;5. N4：响应式时间线与蓝图边界&quot;">​</a></h2><h3 id="n4-0-contract-冻结激活规则" tabindex="-1">N4.0 Contract：冻结激活规则 <a class="header-anchor" href="#n4-0-contract-冻结激活规则" aria-label="Permalink to &quot;N4.0 Contract：冻结激活规则&quot;">​</a></h3><p><strong>Agent 要做（按序）</strong></p><ol><li>阅读产品愿景第 7、10、17 节，<code>src/editor/document.py</code>、<code>src/editor/timeline_commands.py</code>、<code>src/game/events.py</code>、<code>src/game/reactions.py</code>、N2/N3 回归和 scene schema。</li><li>创建 <code>tests/test_activation_rules.py</code>、<code>tests/test_reactive_timeline.py</code>、<code>tests/test_timeline_instance_trace.py</code>，使用最小 Scene/Pattern fixture 先形成红色基线。</li><li>冻结 <code>at_frame</code>、<code>when_variable</code>、<code>on_event</code>、<code>on_lifecycle</code> 的数据表示；冻结变量路径/比较/边沿、事件过滤/密度、来源/owner/终止原因、delay、scope、窗口和取消语义。</li><li>冻结默认 <code>once_per_scope + max_instances=1 + ignore_while_running</code>，显式 <code>restart/parallel</code>，以及未知字段、类型错误、路径错误和未来字段的结构化诊断。</li><li>覆盖死亡开花、假 Boss 受击和背景切换；断言事件只描述事实，不携带 <code>start_clip</code> 等命令。</li></ol><p><strong>完成条件</strong>：Contract 在没有新 runtime 实现时保持可解释的红色预期；规则 round-trip、边沿、重入、延迟、窗口结束、State 退出、owner 和诊断路径均有行为断言。</p><p><strong>验收文件</strong>：<code>tests/test_activation_rules.py</code>、<code>tests/test_reactive_timeline.py</code>、<code>tests/test_timeline_instance_trace.py</code>。</p><p><strong>Evidence（2026-08-10）</strong>：Structural/Runtime：<code>python -m pytest -q tests/test_activation_rules.py tests/test_reactive_timeline.py tests/test_timeline_instance_trace.py</code>（10 passed）；规则 round-trip、变量边沿、固定帧/delay、窗口取消、owner、预算和 runtime identity 均有断言。</p><h3 id="n4-1-runtime-激活、实例、预算与取消" tabindex="-1">N4.1 Runtime：激活、实例、预算与取消 <a class="header-anchor" href="#n4-1-runtime-激活、实例、预算与取消" aria-label="Permalink to &quot;N4.1 Runtime：激活、实例、预算与取消&quot;">​</a></h3><p><strong>Agent 要做（按序）</strong></p><ol><li>将 <code>ReactiveClip</code>/<code>ActivationRule</code> 接入 <code>SceneDocument → src/editor/stage_compile.py → src/game/stage/program.py → StageRunner</code> 正式路径；不建立编辑器专用模拟器。</li><li>按 Inbox → 同帧变量快照 → State 退出/取消优先 → 解析并启动仍有效实例 → 采样轨道的顺序运行；事件 dispatch 不重入。</li><li>分离作者 Clip ID 和运行时 Instance ID；trace 记录 trigger kind/source/event、激活快照、实际触发帧、owner/cancel token、开始/停止原因、并发数、预算拒绝原因和 action resolver 诊断。</li><li>实现 <code>on_rise</code>、<code>while_true</code>、<code>on_fall</code>、<code>on_change</code>、固定帧、动态 delay、事件/生命周期过滤；实例开始后变量变化不能回溯移动实例。</li><li>将固定窗口、条件变假、行为完成、State 退出、显式取消、清场/热重载等终止原因区分开；owner 退出必须传播到 TaskScope、pending reaction 和批量 Action。</li><li>施加最大实例数、每帧生成数、因果深度和批量预算；高密度生命周期不能扩展为逐弹 Python 对象链。</li></ol><p><strong>完成条件</strong>：正式 StageRunner 能跑三类例子；trace 能回答“为什么触发、何时开始、为什么停止、归谁所有”；reset+seek 与固定帧正常播放逐帧等价；作者 <code>to_dict()</code> 和 dirty 状态不变。</p><p><strong>验收文件</strong>：N4.0 三份 Contract、<code>tests/test_reaction_timeline_integration.py</code>；回归 <code>tests/test_reactions.py</code>、<code>tests/test_reaction_scheduler.py</code>、<code>tests/test_state_graph_runtime.py</code>、<code>tests/test_stage_program.py</code> 及 N2/N3 focused gate。</p><p><strong>Evidence（2026-08-10）</strong>：Runtime：<code>python -m pytest -q tests/test_reaction_timeline_integration.py tests/test_reactions.py tests/test_reaction_scheduler.py tests/test_state_graph_runtime.py tests/test_stage_program.py</code>（既有回归与 N4 集成通过）；正式 <code>SceneDocument → compile_stage → StageRunner → PreviewController</code> 路径保持 JSON-only authoring，trace/overlay 不写回文档。</p><h3 id="n4-2-editor-生命周期槽位、徽标、导航、overlay-与冲突" tabindex="-1">N4.2 Editor：生命周期槽位、徽标、导航、overlay 与冲突 <a class="header-anchor" href="#n4-2-editor-生命周期槽位、徽标、导航、overlay-与冲突" aria-label="Permalink to &quot;N4.2 Editor：生命周期槽位、徽标、导航、overlay 与冲突&quot;">​</a></h3><p><strong>Agent 要做（按序）</strong></p><ol><li>修改 <code>src/editor/timeline_workspace.py</code>、<code>src/editor/app.py</code>、<code>src/editor/document.py</code>、<code>src/editor/timeline_commands.py</code>，让 <code>Reactive</code> 成为显式轨道/片段类型并有默认 payload。</li><li>在片段上显示 <code>when/event/lifecycle</code> 徽标、窗口、scope、owner 和简短过滤摘要；点击槽位只导航到 Reaction/Blueprint 局部视图，不把状态图、时间线、行为图合并成一张万能图。</li><li>将 runtime trace 作为只读 overlay 显示 instance、actual start frame、trigger、stop reason、owner、active count、预算拒绝；reset/replay 清理 overlay，不写文档。</li><li>按 <code>target + property + variable</code> 分组显示冲突，能跳回 writer、区间和 reducer；所有增删改都用一个 <code>CommandStack</code> 事务并可 Undo/Redo。</li><li>用真实 PySide6 窗口验证最小尺寸、时间线交互、导航、reset/replay 和 overlay；offscreen 只作为 Structural/Runtime 证据。</li></ol><p><strong>完成条件</strong>：新手能在槽位上理解“何时有效”，高级作者能进入局部行为实现；运行时 overlay 可观察但不污染作者资源；没有蓝图到时间线的隐形长连线。</p><p><strong>验收文件</strong>：<code>tests/test_editor_reactive_clips.py</code>、<code>tests/test_reaction_timeline_integration.py</code>；回归 <code>tests/test_editor_timeline_model.py</code>、<code>tests/test_editor_timeline_workspace.py</code>、<code>tests/test_state_graph_editor.py</code>；另附 native visual 证据。</p><p><strong>Evidence（2026-08-20 重做）</strong>：Structural/Runtime：<code>$env:QT_QPA_PLATFORM=&#39;offscreen&#39;; python -m pytest -q tests/test_editor_reactive_clips.py tests/test_reaction_timeline_integration.py tests/test_editor_timeline_workspace.py tests/test_state_graph_editor.py</code>（通过）；offscreen Qt 仅作为结构和运行证据。Native visual：<code>Remove-Item Env:QT_QPA_PLATFORM -ErrorAction SilentlyContinue; python -u tools/verify_native_editor_n4.py --project . --screenshot $env:TEMP\\pystg-n4-native-editor-script.png</code> 输出 <code>native_editor_n4_ok</code>。该门禁 2026-08-20 重写：不再手写 overlay 载荷，也不再直接调用编辑器的下游槽函数——脚本用编辑器自己的命令创作 Reactive 轨道/片段，<code>compile_stage</code> + <code>StageRunner</code> 真跑一次得到 <code>runner.reactive_overlay</code>，再经 <code>_handle_pattern_preview_event</code> 走编辑器自身的 runtime feedback 管线，最后用真实双击手势进入局部 Reaction 视图；同时验证 overlay 与导航都不写回文档。此次重写暴露并修好一个真实缺陷：编辑器默认 Reactive 片段的窗口只有 1 帧，而 runtime 在片段起始后的帧边界才判定 armed，因此该默认片段永远不会触发（见 <code>tests/test_editor_reactive_clips.py::test_default_reactive_clip_actually_arms_on_the_formal_runtime</code>）。</p><h2 id="_6-n5-可展开的版本化预设" tabindex="-1">6. N5：可展开的版本化预设 <a class="header-anchor" href="#_6-n5-可展开的版本化预设" aria-label="Permalink to &quot;6. N5：可展开的版本化预设&quot;">​</a></h2><h3 id="n5-0-contract-描述、身份和迁移语义" tabindex="-1">N5.0 Contract：描述、身份和迁移语义 <a class="header-anchor" href="#n5-0-contract-描述、身份和迁移语义" aria-label="Permalink to &quot;N5.0 Contract：描述、身份和迁移语义&quot;">​</a></h3><p><strong>Agent 要做</strong>：阅读产品愿景第 11 节、<code>src/pattern</code> 资源/编译器和 N4 规则；创建 <code>tests/test_preset_descriptor.py</code>、<code>tests/test_preset_expansion.py</code>、<code>tests/test_preset_migration.py</code>；冻结 stable preset ID/version、参数 schema、公开插槽、输入/输出变量、事件、虚拟内部 ID、覆盖优先级、精确版本锁、未知字段和迁移失败语义。</p><p><strong>完成条件</strong>：Contract 能拒绝错误类型、缺失版本、未知字段和迁移环；预设实例不会因编辑器升级静默改变。</p><p><strong>验收文件</strong>：<code>tests/test_preset_descriptor.py</code>、<code>tests/test_preset_expansion.py</code>、<code>tests/test_preset_migration.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Structural：三份 Contract 覆盖严格 JSON schema、stable ID、精确 semver、参数/槽位类型、未知字段、精确依赖锁、稳定虚拟身份、迁移环和失败路径；<code>python -m pytest -q tests/test_preset_descriptor.py tests/test_preset_expansion.py tests/test_preset_migration.py</code> 通过。</p><h3 id="n5-1-首发预设库" tabindex="-1">N5.1 首发预设库 <a class="header-anchor" href="#n5-1-首发预设库" aria-label="Permalink to &quot;N5.1 首发预设库&quot;">​</a></h3><p><strong>Agent 要做</strong>：以真实资源提供自机狙、奇数弹、偶数弹、圆形开花、扇形扫射、单/双/交错螺旋、加速旋转、延迟转向、子弹分裂、速度层叠、波纹、米弹墙；每个预设暴露少量精确参数、生命周期策略和预算，走正式 compiler/runner。</p><p><strong>完成条件</strong>：预设可运行、可展开、可调参；与基础 Pattern 行为 parity；高密度路径无逐弹 Python 回调。</p><p><strong>验收文件</strong>：<code>tests/test_preset_library.py</code>、<code>tests/test_pattern_parity.py</code>、<code>tests/test_pattern_compiler.py</code>；附固定 workload profile。</p><p><strong>Evidence（2026-08-13）</strong>：Runtime/Performance：<code>game_content/presets/builtin_patterns.pystg.json</code> 固定 14 个 <code>1.0.0</code> 预设；<code>python tools/profile_n5_presets.py --output C:\\Users\\m1573\\AppData\\Local\\Temp\\pystg-n5-preset-profile.json</code> 在 Windows 11、Python 3.12.7、Intel64 Family 6 Model 151 上运行正式 compiler/runner，1836 发由 101 次批量写入完成，逐弹回调 0，总耗时 41.988ms（回归上限 2500ms）。延迟转向、加速旋转、速度层叠、波纹使用批量曲线；子弹分裂使用 owner 级批量生命周期 action。</p><h3 id="n5-2-虚拟展开" tabindex="-1">N5.2 虚拟展开 <a class="header-anchor" href="#n5-2-虚拟展开" aria-label="Permalink to &quot;N5.2 虚拟展开&quot;">​</a></h3><p><strong>Agent 要做</strong>：只读显示内部发射器、参数和局部行为；虚拟节点 ID 由实例 ID+预设内部 ID 稳定派生；折叠/展开不复制 Scene 节点、不制造 runtime 实例，trace 能从外部实例定位内部节点。</p><p><strong>完成条件</strong>：折叠、展开、保存、重开、reset 的 authoring/runtime identity 一致；没有第二份可漂移文档。</p><p><strong>验收文件</strong>：<code>tests/test_preset_expansion.py</code>、<code>tests/test_editor_preset_workspace.py</code>、<code>tests/test_pattern_graph.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Structural/Native visual：虚拟 ID 由 instance UUID + internal ID 派生，折叠/展开不写 graph、不改变作者 payload，compiled program/replay identity 保留 preset/version/internal IDs；<code>python -u tools/verify_native_editor_n5.py --project . --screenshot C:\\Users\\m1573\\AppData\\Local\\Temp\\pystg-n5-native-editor.png</code> 输出 <code>native_editor_n5_ok presets=14 virtual_nodes=5</code>。</p><h3 id="n5-3-参数覆盖与本地物化" tabindex="-1">N5.3 参数覆盖与本地物化 <a class="header-anchor" href="#n5-3-参数覆盖与本地物化" aria-label="Permalink to &quot;N5.3 参数覆盖与本地物化&quot;">​</a></h3><p><strong>Agent 要做</strong>：公开参数和插槽可覆盖；“展开为本地结构”是可预览、可取消、可 Undo/Redo 的单一 CommandStack 事务；物化后与上游预设断开且不被升级偷偷改写；失败保留原实例。</p><p><strong>完成条件</strong>：覆盖优先级和差异报告可解释；物化、撤销、重做、运行和 trace 可重放。</p><p><strong>验收文件</strong>：<code>tests/test_preset_expansion.py</code>、<code>tests/test_editor_preset_workspace.py</code>、<code>tests/test_editor_authoring_integration.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Editor/Runtime：<code>ApplyPresetCommand</code>、参数/槽位覆盖和 <code>MaterializePresetCommand</code> 全部进入已有 <code>CommandStack</code>；本地化移除上游链接但保留来源审计，Undo/Redo 后正式 <code>PatternCompiler</code> 结果可重放。N5 focused + Pattern/Graph/editor 回归 <code>python -m pytest -q tests/test_preset_descriptor.py tests/test_preset_expansion.py tests/test_preset_migration.py tests/test_preset_library.py tests/test_editor_preset_workspace.py tests/test_pattern_document.py tests/test_pattern_compiler.py tests/test_pattern_parity.py tests/test_pattern_graph.py tests/test_pattern_runtime.py tests/test_lifecycle_batching.py tests/test_lifecycle_events.py tests/test_editor_graph_workspace.py tests/test_editor_authoring_integration.py</code>（103 passed）。</p><p><strong>整改证据（2026-08-20）</strong>：上面这段 Evidence 关于「槽位可覆盖」的部分此前只在命令层成立——<code>SetPresetSlotOverrideCommand</code> 实现完整但全仓零调用者、零测试，作者在编辑器里没有任何入口，等于参数覆盖是真的、插槽覆盖是假的。现在 PatternWorkspace 为每个 slot 渲染 <code>PresetReactionSlotEditor</code>（<code>objectName</code> 为 <code>presetSlot_&lt;slot.id&gt;</code>），启用开关与参数经 <code>presetSlotRequested</code> → <code>_preset_slot_requested</code> 进入同一 <code>CommandStack</code>。<code>tests/test_editor_preset_workspace.py::test_preset_slot_editor_writes_one_undoable_slot_override</code> 用真实 <code>EditorMainWindow</code> 从控件出发断言 <code>slot_overrides</code> 写入、表单重建后回显、两级 Undo 分别回到旧值与无覆盖状态。</p><h3 id="n5-4-精确版本迁移" tabindex="-1">N5.4 精确版本迁移 <a class="header-anchor" href="#n5-4-精确版本迁移" aria-label="Permalink to &quot;N5.4 精确版本迁移&quot;">​</a></h3><p><strong>Agent 要做</strong>：按精确版本执行参数/插槽迁移，在临时副本生成差异和诊断；失败保留原数据、原版本和定位路径；项目依赖锁定到可重放版本，不使用“最接近版本”静默替代。</p><p><strong>完成条件</strong>：成功迁移 round-trip；失败可恢复、可 Undo，原文档不被部分写入。</p><p><strong>验收文件</strong>：<code>tests/test_preset_migration.py</code>、<code>tests/test_preset_descriptor.py</code>、<code>tests/test_pattern_document.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Migration：<code>PresetMigration</code>、<code>PresetDependencyLock</code> 均为严格 JSON round-trip；迁移只在临时 <code>PresetInstance</code> 副本生成稳定 diff，缺源字段/缺链/冲突/循环均给出路径且不改原文档，确认由单个 <code>ApplyPresetMigrationCommand</code> 完成并可 Undo/Redo；解析只接受锁定的精确版本，不做 nearby fallback。</p><p><strong>整改证据（2026-08-20）</strong>：<code>ApplyPresetMigrationCommand</code> 此前只出现在 <code>tests/test_editor_preset_workspace.py</code>，<code>app.py</code> 无任何接线——命令确实单条可撤销，但作者在界面上找不到触发它的地方。现在 PatternWorkspace 显示当前版本并只把「有精确迁移路径」的版本填进 <code>preset_migrate_target</code>，按钮经 <code>presetMigrateRequested</code> → <code>_preset_migrate_requested</code> 先 <code>preview_migration()</code> 再压入一条命令；预览被拒时走 <code>preset_migration_unavailable</code> 诊断而不是崩溃或静默。<code>tests/test_editor_preset_workspace.py::test_preset_migration_button_only_offers_reachable_versions</code> 用真实窗口断言候选只含 <code>2.0.0</code>、点击后实例版本与重命名后的参数正确、到链尾后控件自行禁用、Undo 回到 <code>1.0.0</code>。</p><h2 id="_7-n6-上下文搜索与新手连续流程-不含自然语言" tabindex="-1">7. N6：上下文搜索与新手连续流程（不含自然语言） <a class="header-anchor" href="#_7-n6-上下文搜索与新手连续流程-不含自然语言" aria-label="Permalink to &quot;7. N6：上下文搜索与新手连续流程（不含自然语言）&quot;">​</a></h2><h3 id="n6-0-contract-action-catalog-与新手流程" tabindex="-1">N6.0 Contract：Action Catalog 与新手流程 <a class="header-anchor" href="#n6-0-contract-action-catalog-与新手流程" aria-label="Permalink to &quot;N6.0 Contract：Action Catalog 与新手流程&quot;">​</a></h3><p><strong>Agent 要做</strong>：阅读产品愿景第 10、12、13 节和 N5 接口；创建 <code>tests/test_action_catalog.py</code>、<code>tests/test_contextual_search.py</code>、<code>tests/test_beginner_workflow.py</code>、<code>tests/test_editor_usability.py</code>；冻结 Descriptor→Catalog schema、输入/输出类型、上下文过滤、稳定排序、创建事务、空状态引导和错误定位。自然语言生成明确不在范围。</p><p><strong>完成条件</strong>：Contract 断言实际候选、类型端口、Command/Undo 和空状态，而不是菜单字符串。</p><p><strong>验收文件</strong>：上述四份 Contract。</p><p><strong>Evidence（2026-08-13）</strong>：Structural/UI：<code>ActionDescriptor</code>、<code>ActionCatalog</code>、<code>ActionQuery</code> 和 <code>ActionExecutor</code> 使用正式 Graph 端口表、Scene <code>NodeTypeRegistry</code>、Timeline 类型和 N5 Preset Descriptor；重复 ID、未知 command、上下文/类型/父节点过滤、确定性排序、空状态和明确排除自然语言生成均有行为断言。N6 focused（含 N6.0–N6.3 与编辑器回归）<code>python -m pytest -q tests/test_action_catalog.py tests/test_contextual_search.py tests/test_beginner_workflow.py tests/test_editor_usability.py tests/test_editor_preset_workspace.py tests/test_editor_graph_workspace.py tests/test_editor_timeline_workspace.py tests/test_editor_m3_integration.py tests/test_editor_authoring_integration.py</code>：76 passed。</p><h3 id="n6-1-上下文感知搜索" tabindex="-1">N6.1 上下文感知搜索 <a class="header-anchor" href="#n6-1-上下文感知搜索" aria-label="Permalink to &quot;N6.1 上下文感知搜索&quot;">​</a></h3><p><strong>Agent 要做</strong>：在任意作者上下文轻按空格打开搜索；PointSet 拉线只显示接受 PointSet 的 Action；时间线只显示轨道/片段；场景画布只显示可创建对象；Catalog 来自 Descriptor，UI 不写死分支；解决空格搜索与画布平移冲突并用原生 QA 固定交互。</p><p><strong>完成条件</strong>：无效候选不出现，结果可解释且排序稳定；创建和连接都进入文档 CommandStack。</p><p><strong>验收文件</strong>：<code>tests/test_action_catalog.py</code>、<code>tests/test_contextual_search.py</code>、<code>tests/test_editor_graph_workspace.py</code>、<code>tests/test_editor_timeline_workspace.py</code>；另附 native visual。</p><p><strong>Evidence（2026-08-13）</strong>：Editor/Native visual：Graph、Timeline、Scene 轻按空格打开同一搜索；按住空格配合鼠标恢复 <code>ScrollHandDrag</code>，Graph 输出端口空放携带正式 <code>port_type</code>；结果通过已有命令入口创建并可 Undo。<code>python -u tools/verify_native_editor_n6.py --project . --screenshot C:\\Users\\m1573\\AppData\\Local\\Temp\\pystg-n6-native-editor.png</code> 在真实 PySide6 Windows 窗口通过：<code>native_editor_n6_ok scene_candidates=8 graph_nodes=6</code>；截图人工复核 L2 单一渐进导航、精确绑定控件和原生布局可见。</p><p><strong>整改证据（2026-08-14）</strong>：Graph 端口改为高辨识度 <code>I/O</code> 标记并扩大边缘命中区；端口代理接管 press/move/release，拖线时显示临时连线和合法/非法目标反馈，不再误移动节点。<code>tests/test_editor_graph_workspace.py</code> 使用真实 viewport 鼠标事件验证抓取、指针跟随、合法目标高亮、完整连线、节点位置不变及重建安全；Windows 原生 PySide6 鼠标验收另行验证删除连线、重新连接和 Undo/Redo。上下文搜索仍由正式端口类型过滤并通过同一 <code>CommandStack</code> 创建。本项重新关闭。</p><h3 id="n6-2-道中-关底骨架模板" tabindex="-1">N6.2 道中/关底骨架模板 <a class="header-anchor" href="#n6-2-道中-关底骨架模板" aria-label="Permalink to &quot;N6.2 道中/关底骨架模板&quot;">​</a></h3><p><strong>Agent 要做</strong>：项目向导生成道中和两阶段 Boss 骨架（State、Timeline、Background、Pattern、音频和预览入口）；模板创建是一个可 Undo 的文档事务；缺资源、条件错误和 runtime error 定位到资源/属性/规则。</p><p><strong>完成条件</strong>：新人无需理解事件队列、内部 ID 或插件注册即可完成首个 Pattern、全灭后续和背景转场。</p><p><strong>验收文件</strong>：<code>tests/test_beginner_workflow.py</code>、<code>tests/test_editor_m3_integration.py</code>、<code>tests/test_editor_authoring_integration.py</code>、<code>tests/test_editor_timeline_workspace.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Runtime/Authoring：Add 菜单提供道中和两阶段 Boss 骨架；<code>ApplyStageTemplateCommand</code> 在同一 <code>SceneDocument</code> 中一次创建 State、Pattern、Movement、Audio、正式 Background 和 Reactive 轨道并整体 Undo/Redo。<code>encounter.cleared</code> 由 <code>stage.state.complete</code> 在下一固定帧请求 State Graph 转移；背景通过受限 <code>request_background_transition</code>，不伪装脚本。<code>tests/test_beginner_workflow.py</code> 5 passed；正式 <code>compile_stage → StageRunner</code>、Pattern 资源、背景动作、错误路径和全灭后续均有断言，N6 focused 76 passed。</p><p><strong>整改证据（2026-08-14）</strong>：中文模式创建的两阶段 Boss 直接保存作者可读的“两阶段 Boss、登场、通常阶段、强化阶段、结束、背景、背景音乐、背景转场、关卡背景音乐”，内部 kind/ID/runtime 枚举仍保持稳定英文；模板标题和 Undo 显示“撤销 创建两阶段 Boss 模板”。时间线按常见习惯支持空白定位播放头、主体拖动、左右边缘修剪、<code>Ctrl+滚轮</code> 缩放、<code>Shift+滚轮</code> 横移及 Undo/Redo，所有几何修改仍进入 <code>CommandStack</code>。原生 gate <code>python -u tools/verify_native_editor_n6.py --project . --screenshot %TEMP%\\pystg-n6-native-zh.png --compact-screenshot %TEMP%\\pystg-n6-native-zh-960x640.png</code> 通过，分别验证中文 1480×920 和带真实模板轨道的 960×640；最小窗口保留两行工具栏和可编辑轨道，无面板堆叠。本项重新关闭。</p><h3 id="n6-3-分层展开与连续编辑" tabindex="-1">N6.3 分层展开与连续编辑 <a class="header-anchor" href="#n6-3-分层展开与连续编辑" aria-label="Permalink to &quot;N6.3 分层展开与连续编辑&quot;">​</a></h3><p><strong>Agent 要做</strong>：以“选择预设→调整参数→添加动态变化→编辑节点→查看脚本源码”的任务语言提供连续入口、返回和权限提示；内部层级 ID 只用于稳定状态，不作为作者界面术语。所有层级共享同一生命周期、类型、owner 和 debug identity；局部替换不要求推倒重写。</p><p><strong>完成条件</strong>：从调参到局部实现是连续操作；折叠/展开不复制资源、不产生第二套 runtime。</p><p><strong>验收文件</strong>：<code>tests/test_beginner_workflow.py</code>、<code>tests/test_editor_graph_workspace.py</code>、<code>tests/test_editor_preset_workspace.py</code>、<code>tests/test_editor_usability.py</code>。</p><p><strong>Evidence（2026-08-13）</strong>：Editor/Runtime：PatternWorkspace 以单一 L0–L4 入口显示 Preset、精确参数、Binding、Behavior Graph 与 Runtime 源码定位；旧 Recipe/Graph 控件只保留兼容对象并从作者 UI 隐藏。L2 binding 和 L3 expand 均走 CommandStack；返回 L2 不折叠、不复制 Graph，资源 ID、正式编译 identity、owner/lifecycle 契约保持同一 Pattern。<code>tests/test_editor_usability.py</code>、Preset/Graph 回归及 N6 focused 76 passed；原生 N6 gate 通过。</p><p><strong>整改证据（2026-08-14）</strong>：作者导航现只显示“选择预设→调整参数→添加动态变化→编辑节点→查看脚本源码”，不暴露 L0–L4、Behavior Graph 或 Runtime Source；中文动态设置显示“每轮子弹数 / 固定值”等任务词，内部仍保存 <code>shape.count / constant</code>。预设、参数、动态设置、节点和脚本入口共用同一 Pattern 资源、正式 compiler identity 与 Undo/Redo，往返不复制 Graph。N6 focused gate（Action、搜索、新手流程、预设、Graph、时间线、中文、M3 与作者集成）88 passed；原生中文 1480×920/960×640 gate 通过。本项重新关闭。</p><p><strong>整改证据（2026-08-20）</strong>：上面这段整改只覆盖了导航控件，主界面文案仍有残留：<code>GraphPlaceholder</code> 硬编码 “This pattern is in Recipe mode. Expand it into the typed behavior graph…” 与按钮 “Expand to Graph”，<code>src/editor/i18n.py</code> 又把它逐字译成“配方模式 / 类型化行为图 / 展开为行为图”——被声称消灭的术语由 i18n 字典忠实保存了下来；<code>fold_button</code> 也还写着 “Fold back to Recipe”。现在两侧一并改为任务语言（占位页说明「此弹幕目前只由参数描述。以节点方式打开后，即可编辑每一步以及它们之间的连接。」，按钮为 “Edit Nodes” / 「编辑节点」，折返按钮为 “Back to Parameters” / 「返回调整参数」），并删除仅为兼容测试而隐藏保留的 <code>mode_switch</code>。根因是缺少端到端文案审查，因此新增 <code>tests/test_editor_i18n_coverage.py::test_chinese_shell_leaves_no_author_facing_string_untranslated</code>：遍历真实编辑器控件树，凡 i18n 层声称拥有却译不出的作者可见文案即失败，<code>test_coverage_walk_actually_sees_the_shell</code> 反向保证这条遍历确实走到了 shell 而不是空转。</p><h2 id="_8-er-n7-前的编辑器架构整改" tabindex="-1">8. ER：N7 前的编辑器架构整改 <a class="header-anchor" href="#_8-er-n7-前的编辑器架构整改" aria-label="Permalink to &quot;8. ER：N7 前的编辑器架构整改&quot;">​</a></h2><p>ER 的目标结构、允许依赖、状态所有权和目录职责见 <a href="./EDITOR_ARCHITECTURE">EDITOR_ARCHITECTURE.md</a>。每项只能按表中路径工作；路径尚不存在时由对应任务创建，禁止提前创建空目录或无调用者占位实现。</p><p>ER 不改变作者文档语义、时间线/行为图产品模型、正式 runtime、Renderer API 或 N7 权限模型。任何必须改变这些契约的发现都应停止并请求维护者决定。</p><h3 id="er0-文档、边界-contract-与真实基线" tabindex="-1">ER0 文档、边界 Contract 与真实基线 <a class="header-anchor" href="#er0-文档、边界-contract-与真实基线" aria-label="Permalink to &quot;ER0 文档、边界 Contract 与真实基线&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[x]</code>；依赖 N6.3。</p><p><strong>允许路径</strong>：<code>AGENTS.md</code>、<code>docs/EDITOR_ARCHITECTURE.md</code>、本文、<code>tests/test_editor_architecture_boundaries.py</code>、现有架构/回归测试、现有 native verifier 的验收步骤。</p><p><strong>禁止路径</strong>：<code>src/</code>、<code>game_content/</code> 和运行时产品实现。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>核对目标目录、允许/禁止依赖、Agent 所有权和 ER0–ER8 顺序，解决文档冲突。</li><li>创建 <code>tests/test_editor_architecture_boundaries.py</code>，用 AST/import graph 和运行时 Protocol 断言目标边界：<code>src.authoring</code>、<code>src.compiler</code>、<code>src.preview</code> 与 runtime headless 包不导入 editor/Qt，Panel 不导入领域 Command，图形模块无循环，窗口无 SlotsMixin/裸预览进程/双 registry，临时状态有类型。</li><li>Contract 必须对当前真实问题形成精确红色基线；不允许只搜索一段固定源码字符串或把当前错误加入永久 allowlist。</li><li>在同一 checkout 记录 full suite、compileall、assets、diff-check、正式 preview、1480×920/960×640 native 和固定 Pattern workload。无法运行的证据必须写明环境和 blocker。</li></ol><p><strong>完成条件</strong>：三份文档一致；Contract 对当前已知问题逐项失败且没有额外模糊失败；基线证据可复现。ER0 是 Contract 任务，预期红色断言不阻止进入 ER1，但不得合并一个只含红测试、没有后续整改的 <code>main</code>。</p><p><strong>验收文件</strong>：<code>tests/test_editor_architecture_boundaries.py</code>、<code>tests/test_architecture_contracts.py</code>、<code>tests/test_editor_regression_contracts.py</code>、<code>tools/verify_native_editor_n6.py</code>。</p><p><strong>Evidence（2026-08-20）</strong>：环境为 Windows 11 <code>10.0.26200</code>、Python 3.12.7、PySide6 6.8.1.1、Intel64 Family 6 Model 151；PATH 首位 <code>python.exe</code> 是不可用的 WindowsApps shim，因此以下命令固定使用 <code>C:\\Users\\m1573\\anaconda3\\python.exe</code>。Structural：<code>$env:QT_QPA_PLATFORM=&#39;offscreen&#39;; C:\\Users\\m1573\\anaconda3\\python.exe -m pytest -o addopts= -q tests/test_editor_architecture_boundaries.py tests/test_architecture_contracts.py tests/test_editor_regression_contracts.py</code> 得到 125 passed、8 个预期 Contract 红；红项精确对应 authoring/preview 反向依赖、graph/pattern 循环、八个 <code>SlotsMixin</code>、裸 preview <code>QProcess</code>、双 registry、无类型临时状态和缺失 <code>AuthoringDocument</code> Protocol，既有两份验收文件单独为 106/106 passed。Contract 共 27 项，使用 AST/import graph 与运行时 Protocol，覆盖 <code>from ... import ...</code> 别名边、<code>src.authoring.commands.*</code> 和未注解 preview <code>QProcess</code>，没有固定产品源码字符串、永久错误 allowlist、<code>skip</code> 或 <code>xfail</code>；独立验证 Agent 只读复验后批准 ER0。Runtime：Contract 写入前同一 product checkout 的 <code>$env:QT_QPA_PLATFORM=&#39;offscreen&#39;; C:\\Users\\m1573\\anaconda3\\python.exe -m pytest -q</code> 为既有 641 passed；当前总收集 668（641 既有 + 27 ER0），正式 preview controller/protocol/process/editor integration 为 21/21 passed；<code>C:\\Users\\m1573\\anaconda3\\python.exe -m compileall -q main.py src game_content tools tests</code> 通过，<code>validate_assets.py --format json</code> 检查 74 JSON、16 sprite configs、745 sprites、142 images，0 error/warning，<code>git diff --check</code> 通过。Native visual/runtime：清除 <code>QT_QPA_PLATFORM</code> 后，<code>verify_native_editor_n6.py</code> 的真实 PySide6 1480×920 与 960×640 门禁通过，截图为 <code>%TEMP%\\pystg-er0-native-1480x920.png</code> 和 <code>%TEMP%\\pystg-er0-native-960x640.png</code>，人工复核无面板堆叠且时间线可编辑；<code>verify_native_editor_n4.py</code> 通过正式 <code>compile_stage → StageRunner → runtime feedback</code> 得到 1 条 Reactive trace，截图为 <code>%TEMP%\\pystg-er0-native-n4-preview.png</code>。Performance：<code>profile_n5_presets.py --output %TEMP%\\pystg-er0-preset-profile.json</code> 得到 14 presets、1836 spawned、101 batch writes、0 per-bullet callbacks、53.67 ms（上限 2500 ms）。Usability：not run，按顺序须在 ER8 后执行 N6.4。ER0 自身无 blocker；这组预期红 Contract 必须随 ER1–ER7 整改继续保留，不得作为仅含红测试的提交单独合入 <code>main</code>。</p><h3 id="er1-有类型的编辑状态" tabindex="-1">ER1 有类型的编辑状态 <a class="header-anchor" href="#er1-有类型的编辑状态" aria-label="Permalink to &quot;ER1 有类型的编辑状态&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[x]</code>；依赖 ER0。</p><p><strong>允许路径</strong>：<code>src/editor/state/</code>、<code>src/editor/document_manager.py</code>、当前读取 <code>editor_context</code> 的 <code>src/editor/app.py</code> 与 <code>src/editor/main_window_*.py</code>、focused tests；native 验收发现旧状态访问后，主协调 Agent 将边界单文件扩展到 <code>tools/verify_native_editor_n4.py</code>。</p><p><strong>禁止路径</strong>：作者 schema、compiler、runtime、renderer、Panel 产品行为和 N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>定义 <code>SelectionState</code>、<code>TimelineViewState</code>、<code>PatternViewState</code>、<code>DocumentEditorState</code> 和 <code>RuntimeOverlayState</code>；字段类型、默认值和清理时机必须与架构文档一致。</li><li>每个 <code>ManagedDocument</code> 只拥有选择/视图状态；运行时 overlay 改由活动预览所有者管理，不进入作者文档。</li><li>迁移全部 22 个隐式键；删除目标时校正选择，切换文档时恢复各自状态，stop/reset 只清理 overlay。</li><li>保持 Undo/Redo、dirty、canonical JSON、autosave/recovery 和文档切换行为不变。</li></ol><p><strong>硬指标</strong>：生产代码中 <code>editor_context</code> 和相关字符串键为零；两个文档的选择/播放头互不污染；overlay 不序列化、不制造 dirty；状态错误在构造或赋值边界明确失败。</p><p><strong>验收文件</strong>：新建 <code>tests/test_editor_state_contract.py</code>；回归 <code>tests/test_editor_document_manager.py</code>、<code>tests/test_editor_reactive_clips.py</code>、<code>tests/test_preview_editor_integration.py</code>。</p><p><strong>Evidence（2026-08-20）</strong>：环境为 Windows 11 <code>10.0.26200</code>、Python 3.12.7、PySide6 6.8.1.1、Intel64 Family 6 Model 151；命令固定使用 <code>C:\\Users\\m1573\\anaconda3\\python.exe</code>。Structural：新建冻结 <code>tests/test_editor_state_contract.py</code>，逐键确认 18 个文档状态和 4 个 preview overlay 状态；<code>$env:QT_QPA_PLATFORM=&#39;offscreen&#39;; python -m pytest -o addopts= -q tests/test_editor_state_contract.py tests/test_editor_document_manager.py tests/test_editor_reactive_clips.py tests/test_preview_editor_integration.py tests/test_contextual_search.py tests/test_editor_authoring_integration.py tests/test_editor_graph_workspace.py tests/test_editor_m3_integration.py tests/test_editor_timeline_workspace.py tests/test_state_graph_editor.py</code> 为 103 passed，其中 ER1 Contract 为 25/25；<code>tests/test_editor_architecture_boundaries.py tests/test_architecture_contracts.py tests/test_editor_regression_contracts.py</code> 为 126 passed、精确保留 7 个 ER2–ER7 预期红项，ER0 的 typed-state 红项已转绿。Runtime：同一 checkout 全量 <code>python -m pytest -o addopts= -q</code> 为 686 passed、仅上述 7 个预期 Contract 红项；正式 preview controller/protocol/process/editor integration 为 21/21 passed。<code>ManagedDocument</code> 只拥有一个 <code>DocumentEditorState</code>；双文档选择/播放头隔离，删除、Undo/Redo、revert/close 会校正或释放 transient state；不可变 <code>RuntimeOverlayState</code> 由 preview owner 按文档持有，stop/reset/error/进程或 owner 关闭只清 overlay，不改变作者播放头、canonical JSON、dirty 或 history。生产 <code>src/editor</code> 与 N4 verifier 中 <code>editor_context</code> 和 22 个旧字符串键为零，无旧 payload fallback、<code>skip</code> 或 <code>xfail</code>。<code>python -m compileall -q main.py src game_content tools tests</code> 通过；资产检查 74 JSON、16 sprite configs、745 sprites、142 images，0 error/warning；<code>git diff --check</code> 通过。Native visual/runtime：真实 PySide6 <code>verify_native_editor_n6.py</code> 在 1480×920 与 960×640 通过，截图为 <code>%TEMP%\\pystg-er1-native-1480x920.png</code>、<code>%TEMP%\\pystg-er1-native-960x640.png</code>，人工复核无面板堆叠且紧凑时间线可编辑；<code>verify_native_editor_n4.py</code> 经单文件 typed-state 迁移后，正式 <code>compile_stage → StageRunner → statistics event</code> 得到真实 <code>runner.frame=1</code> 和 1 条 Reactive trace，真实双击导航通过，截图 <code>%TEMP%\\pystg-er1-native-n4.png</code> 中激活片段、Frame 1 和检查器可见。Performance：<code>profile_n5_presets.py --output %TEMP%\\pystg-er1-preset-profile.json</code> 得到 14 presets、1836 spawned、101 batch writes、0 per-bullet callbacks、51.824 ms（上限 2500 ms）。Usability：not run，依赖链规定在 ER8 后执行 N6.4。独立只读验证 Agent 复验 Contract、native N4、N6 双尺寸、性能及工具修复后批准 ER1；无 blocker。工作树位于 <code>main</code> 且未提交，既有 <code>.claude/settings.local.json</code>、AGENTS/架构文档与 ER0 Contract 改动均保留；ER1 未修改作者 schema、compiler、runtime、renderer、Panel 产品行为或 N7。</p><h3 id="er2-coordinator-与局部失效" tabindex="-1">ER2 Coordinator 与局部失效 <a class="header-anchor" href="#er2-coordinator-与局部失效" aria-label="Permalink to &quot;ER2 Coordinator 与局部失效&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER1。</p><p><strong>允许路径</strong>：<code>src/editor/application/</code>、<code>src/editor/shell/</code>、<code>src/editor/app.py</code>、<code>src/editor/main_window_*.py</code>、<code>src/editor/state/</code>、focused tests。</p><p><strong>禁止路径</strong>：作者 schema、compiler/runtime、具体 Panel 绘制和交互实现、N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>定义不携带 QWidget 的 <code>EditorIntent</code>、公开 Panel Port 和有限 <code>InvalidationSet</code>。</li><li>引入 <code>EditorCoordinator</code> 与 <code>DocumentController</code>；由 Coordinator 验证意图、创建/提交 Command、更新临时状态并返回局部失效。</li><li>将窗口领域槽迁出八个 <code>SlotsMixin</code>；<code>EditorMainWindow</code> 只组装 Qt、连接公开接口和关闭顶层服务。</li><li>普通 mutation 只刷新受影响的 Panel；完整同步只允许在打开/切换文档和迁移后重新绑定。</li></ol><p><strong>硬指标</strong>：窗口不继承 <code>SlotsMixin</code>，不导入领域 Command，不调用 Panel 私有方法；mutation handler 不调用全局 <code>_refresh()</code>；一次用户提交只产生一个 Command；Undo/Redo 经过同一协调路径。</p><p><strong>验收文件</strong>：新建 <code>tests/test_editor_coordinator.py</code>；回归 <code>tests/test_editor_app_smoke.py</code>、<code>tests/test_editor_authoring_integration.py</code>、<code>tests/test_editor_scene_commands.py</code>、<code>tests/test_editor_timeline_commands.py</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h3 id="er3-authoring、command-与-compiler-包边界" tabindex="-1">ER3 Authoring、Command 与 Compiler 包边界 <a class="header-anchor" href="#er3-authoring、command-与-compiler-包边界" aria-label="Permalink to &quot;ER3 Authoring、Command 与 Compiler 包边界&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER2。</p><p><strong>允许路径</strong>：<code>src/authoring/</code>、新建 <code>src/compiler/</code>、现有无 Qt 的 <code>src/editor/document.py</code>、<code>commands.py</code>、<code>*_commands.py</code>、<code>stage_compile.py</code>、<code>scene_compile.py</code> 及其兼容出口、<code>src/preview/</code> 的 import 适配、focused tests。</p><p><strong>禁止路径</strong>：Qt Shell/Panel 行为、runtime 语义、renderer 和 N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>定义 <code>AuthoringDocument</code> Protocol 和明确的 Scene/Pattern/UI/Background 支持类型。</li><li>将 Scene 作者数据移到 <code>src/authoring/scene/</code>，领域 Command 移到 <code>src/authoring/commands/</code>。</li><li>将 Stage/Spell compiler 和诊断移到 <code>src/compiler/</code>，提供与现有正式运行路径一致的 facade。</li><li>旧 <code>src/editor/*</code> 路径先变为无逻辑 re-export；逐个迁移内部调用者，不复制实现。</li><li>将 headless 资源 registry 与 Qt editor factory 解耦；Qt contribution 留给 ER5。</li></ol><p><strong>硬指标</strong>：<code>src.authoring</code>、<code>src.compiler</code>、<code>src.preview</code> 不导入 <code>src.editor</code> 或 Qt；Command 无 Qt；四类文档都能在无 Qt 环境加载/验证；迁移前后 canonical JSON、编译结果和 runtime identity 一致。</p><p><strong>验收文件</strong>：<code>tests/test_editor_architecture_boundaries.py</code>、<code>tests/test_editor_documents.py</code>、<code>tests/test_editor_scene_compile.py</code>、<code>tests/test_editor_document_manager.py</code>、<code>tests/test_preview_controller.py</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h3 id="er4-唯一-previewsession" tabindex="-1">ER4 唯一 PreviewSession <a class="header-anchor" href="#er4-唯一-previewsession" aria-label="Permalink to &quot;ER4 唯一 PreviewSession&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER3。</p><p><strong>允许路径</strong>：<code>src/editor/preview/</code>、<code>src/preview/</code>、当前 <code>src/editor/preview_process.py</code>、<code>runtime_preview.py</code>、预览相关 shell 接线与 focused tests。</p><p><strong>禁止路径</strong>：作者 schema、时间线/行为图实现、renderer 语义、插件和 N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>建立唯一 <code>PreviewSession</code>，明确 <code>formal_authoring</code> 与 <code>legacy_game_run</code> 模式和共同状态机。</li><li>收拢进程、协议、宿主、活动文档 identity、启动/停止/错误/超时/输出上限和关闭。</li><li>窗口不再持有裸预览 <code>QProcess</code>；<code>RuntimePreviewHost</code> 只嵌入，不拥有进程。</li><li>外部编辑工具进程保持独立，不伪装成游戏预览。</li></ol><p><strong>硬指标</strong>：同一时间只有一个活动预览；反馈不会路由到错误文档；stop/reset/crash/close 清理进程和 overlay；legacy 结果不标为 formal；正式 Pattern/Stage 仍经 JSON 协议和正式 runtime。</p><p><strong>验收文件</strong>：新建 <code>tests/test_preview_session.py</code>；回归 <code>tests/test_preview_process.py</code>、<code>tests/test_preview_protocol.py</code>、<code>tests/test_preview_editor_integration.py</code>、native preview gate。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h3 id="er5-唯一插件贡献入口" tabindex="-1">ER5 唯一插件贡献入口 <a class="header-anchor" href="#er5-唯一插件贡献入口" aria-label="Permalink to &quot;ER5 唯一插件贡献入口&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER4。</p><p><strong>允许路径</strong>：<code>src/editor/plugins/</code>、当前 <code>src/editor/plugin_sdk.py</code>、<code>workbench.py</code>、headless registry 的 Tool 适配点、focused tests。</p><p><strong>禁止路径</strong>：窗口私有状态、runtime 内部、作者 schema、Panel 产品行为和 N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>窗口只依赖一个 <code>EditorPluginRegistry</code> facade。</li><li>用适配器接入 headless SDK contribution 和 Qt workbench/external tool contribution。</li><li>统一插件 identity、依赖、激活状态、失败回滚、停用和清理；禁止两个互不知情的生命周期。</li><li>插件上下文不能取得窗口、内部 registry 或全局 runtime 对象。</li></ol><p><strong>硬指标</strong>：窗口不再同时持有 <code>plugin_registry</code> 与 <code>plugin_sdk_registry</code>；部分激活失败完整回滚；停用撤销所有 owned contribution；外部工具关闭不影响 formal preview。</p><p><strong>验收文件</strong>：新建或收敛 <code>tests/test_editor_plugin_registry.py</code>；回归 <code>tests/test_plugin_sdk.py</code>、<code>tests/test_editor_workbench.py</code>、<code>tests/test_editor_regression_contracts.py</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h3 id="er6-panel-边界与图形循环" tabindex="-1">ER6 Panel 边界与图形循环 <a class="header-anchor" href="#er6-panel-边界与图形循环" aria-label="Permalink to &quot;ER6 Panel 边界与图形循环&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER5。</p><p><strong>允许路径</strong>：<code>src/editor/panels/</code>、<code>src/editor/graphics/</code>、待迁移的当前 workspace/view/panel 文件、公开 Intent/Port 接线、focused tests。</p><p><strong>禁止路径</strong>：Coordinator 内部、作者 schema、compiler/runtime、preview/plugin 生命周期和 N7；接口不够时必须停止并交回 ER2 所有者修订。</p><p><strong>子任务与独立所有者</strong></p><ul><li>ER6.0：抽出共享 canvas/graph 图元，消除 <code>graph_workspace ↔ pattern_workspace</code> 循环。</li><li>ER6.1：迁移 Scene 与 Inspector。</li><li>ER6.2：迁移 Timeline、State Graph 与 Variables。</li><li>ER6.3：迁移 Pattern 与 Behavior Graph。</li><li>ER6.4：迁移 UI 与 Background。</li></ul><p>每个子任务只能由一个 Panel Agent 修改其列出的 Panel；上一个 focused gate 通过后才能领取下一个。</p><p><strong>硬指标</strong>：Panel 之间无具体实现导入和私有调用；Panel 不直接修改文档或 push Command；释放鼠标只提交一次 Intent；图连线、节点拖动、时间线移动/修剪和 UI gizmo 均可 Undo/Redo。</p><p><strong>验收文件</strong>：新建 <code>tests/test_editor_panel_boundaries.py</code>；回归 <code>tests/test_editor_graph_workspace.py</code>、<code>tests/test_editor_timeline_workspace.py</code>、<code>tests/test_editor_preset_workspace.py</code>、<code>tests/test_editor_m6_workspace.py</code>、<code>tests/test_editor_m6_integration.py</code> 及真实鼠标 native gate。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：ER6.0–ER6.4 全部完成前保持 <code>[ ]</code>。</p><h3 id="er7-兼容层、schema-与重复生命周期清理" tabindex="-1">ER7 兼容层、schema 与重复生命周期清理 <a class="header-anchor" href="#er7-兼容层、schema-与重复生命周期清理" aria-label="Permalink to &quot;ER7 兼容层、schema 与重复生命周期清理&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER6。</p><p><strong>允许路径</strong>：<code>src/authoring/</code>、<code>src/compiler/</code>、旧 <code>src/editor/</code> 兼容出口、<code>src/editor/session.py</code>、<code>document_manager.py</code>、schema/migration 和 focused tests。</p><p><strong>禁止路径</strong>：新增产品能力、runtime/renderer 语义、Panel 重新设计和 N7。</p><p><strong>Agent 要做（按序）</strong></p><ol><li>将 <code>CURRENT_SCHEMA_VERSION</code> 恢复为普通整数 4；v3 只通过显式 v3→v4 migration 读取。</li><li>收敛 <code>SceneEditorSession</code>：内部调用者归零后降为兼容工厂或删除，不再保留第二套 open/save/Undo 生命周期。</li><li>核对所有旧 re-export 的内部调用者；只删除引用为零且有明确替代路径的出口。</li><li>清理 Mixin、重复逻辑和迁移期 allowlist；不删除仍有外部兼容责任的公共入口。</li></ol><p><strong>硬指标</strong>：版本 4 不与 3 相等；旧文档显式迁移且 round-trip；仓库内部不依赖旧 editor 文档/compiler/command 路径；一个文档只有一个生命周期和 CommandStack。</p><p><strong>验收文件</strong>：新建 <code>tests/test_editor_schema_migration.py</code>；回归 <code>tests/test_scene_v4_contract.py</code>、<code>tests/test_editor_documents.py</code>、<code>tests/test_editor_document_manager.py</code>、<code>tests/test_editor_regression_contracts.py</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h3 id="er8-架构整体验收" tabindex="-1">ER8 架构整体验收 <a class="header-anchor" href="#er8-架构整体验收" aria-label="Permalink to &quot;ER8 架构整体验收&quot;">​</a></h3><p><strong>状态/依赖</strong>：<code>[ ]</code>；依赖 ER7。该任务由未参与 ER1–ER7 实现的验证 Agent 执行，产品代码只读。</p><p><strong>允许路径</strong>：门禁命令、验收报告和本文当前 Evidence/Blocker。任何产品修复必须退回对应 ER 所有者，ER8 不得边验收边修改。</p><p><strong>必须完成的证据</strong></p><ol><li>Structural：架构边界 Contract 全绿、无循环、无禁止 import、兼容 allowlist 清空或逐项说明。</li><li>Runtime：focused + N2/N3 + 全量 suite、compileall、assets、diff-check。</li><li>Native visual：真实 PySide6 1480×920/960×640，中英文切换，无堆叠，正常启动和关闭。</li><li>Native interaction：场景选择/拖动、时间线定位/移动/修剪/缩放、Graph 真实拖线、Undo/Redo。</li><li>Formal preview：Pattern/Stage 加载、play/pause/seek/reset、overlay identity、异常退出和编辑器关闭。</li><li>Performance：固定 N5 workload 批量写入保持、逐弹回调为零，记录目标硬件、峰值和阈值。</li><li>Clean checkout：从干净 checkout 重跑，不依赖未提交文件、当前工作目录或手工预热状态。</li></ol><p><strong>完成条件</strong>：所有证据类别分别通过且没有残余 blocker。offscreen、截图、单一 pytest 数字或实现 Agent 自报不能替代对应证据。ER8 通过只允许开始 N6.4，不允许直接开始 N7。</p><p><strong>验收文件</strong>：ER0–ER7 所有 focused 文件、现有 N2/N3/N6 gate、<code>tools/verify_native_editor_n4.py</code>、<code>tools/verify_native_editor_n5.py</code>、<code>tools/verify_native_editor_n6.py</code>、<code>tools/profile_n5_presets.py</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：保持 <code>[ ]</code>。</p><h2 id="_9-n6-4-usability-gate" tabindex="-1">9. N6.4：Usability gate <a class="header-anchor" href="#_9-n6-4-usability-gate" aria-label="Permalink to &quot;9. N6.4：Usability gate&quot;">​</a></h2><p><strong>前置条件</strong>：ER8 已通过。在 ER8 之前不得邀请五名用户，避免在仍会发生结构迁移的版本上浪费人工样本。</p><p><strong>Agent 要做</strong>：邀请至少 5 名没有 PySTG 经验的目标用户，记录首个 Pattern、全灭后续、两阶段 Boss+背景切换、撤销/重开/预览和一次局部展开的时间、失败点和求助次数；维护者不能口头带做。</p><p><strong>完成条件</strong>：至少 4/5 用户在 10 分钟内完成可运行 Pattern、30 分钟内完成短道中、60 分钟内完成两阶段 Boss、一次背景转场和一次事件反应，全程不写脚本。</p><p><strong>验收文件</strong>：<code>tests/test_editor_usability.py</code>、N6 focused gate、真实 PySide6 截图和 <code>reports/n6_usability.json</code>。</p><p><strong>Evidence / Blocker（尚未完成）</strong>：研究协议已固定在 <code>docs/N6_USABILITY_PROTOCOL.md</code>，报告由 <code>tools/verify_n6_usability.py</code> 严格校验 5 名唯一、无 PySTG 经验、无维护者口头指导、无脚本及 4/5 时间阈值。此前四类 UI 阻塞已有自动化和原生证据，但 2026-08-20 架构复审新增 ER0–ER8 前置条件；当前没有报告且 ER8 未完成，本项保持 <code>[ ]</code>，N7 不得开始。</p><h2 id="_10-n7-behavior-descriptor-与-safe-runtime-engine-权限" tabindex="-1">10. N7：Behavior Descriptor 与 Safe/Runtime/Engine 权限 <a class="header-anchor" href="#_10-n7-behavior-descriptor-与-safe-runtime-engine-权限" aria-label="Permalink to &quot;10. N7：Behavior Descriptor 与 Safe/Runtime/Engine 权限&quot;">​</a></h2><h3 id="n7-0-contract-统一-descriptor" tabindex="-1">N7.0 Contract：统一 Descriptor <a class="header-anchor" href="#n7-0-contract-统一-descriptor" aria-label="Permalink to &quot;N7.0 Contract：统一 Descriptor&quot;">​</a></h3><p><strong>Agent 要做</strong>：阅读产品愿景第 5、14、15 节、插件 SDK、资源 registry 和 N6 Catalog；创建 <code>tests/test_behavior_descriptor.py</code>、<code>tests/test_safe_capabilities.py</code>、<code>tests/test_plugin_package_boundaries.py</code>、<code>tests/test_behavior_plugin_integration.py</code>；冻结 stable ID/version、typed ports、参数 schema、capabilities、lifecycle/cancel、debug snapshot、Tool contribution 和 manifest 错误。</p><p><strong>完成条件</strong>：headless runtime 能拒绝越权；同一 Descriptor 的 schema/runtime/tool identity 一致。</p><p><strong>验收文件</strong>：上述四份 Contract。</p><h3 id="n7-1-safe-api" tabindex="-1">N7.1 Safe API <a class="header-anchor" href="#n7-1-safe-api" aria-label="Permalink to &quot;N7.1 Safe API&quot;">​</a></h3><p><strong>Agent 要做</strong>：只开放批量发射、授权移动、资源播放、只读 snapshot、类型化等待和局部变量；不返回 Pool/Renderer/Manager，不允许文件、网络、进程或模块访问。明确进程内 Python 是受信任 Runtime，不冒充沙箱。</p><p><strong>完成条件</strong>：manifest 和运行时能力检查都拒绝越权；Safe 行为可热重载、可取消、可 debug。</p><p><strong>验收文件</strong>：<code>tests/test_safe_capabilities.py</code>、<code>tests/test_behavior_descriptor.py</code>、<code>tests/test_editor_regression_contracts.py</code>。</p><h3 id="n7-2-runtime-api" tabindex="-1">N7.2 Runtime API <a class="header-anchor" href="#n7-2-runtime-api" aria-label="Permalink to &quot;N7.2 Runtime API&quot;">​</a></h3><p><strong>Agent 要做</strong>：允许注册组件、事件、弹道、碰撞、道具和受限 Renderer Pass；声明线程、所有权、清理和 replay policy；禁止逐弹回调、任意全局状态和未声明输出。</p><p><strong>完成条件</strong>：Runtime 插件可服务 N4/N5 行为，失败和取消可追踪，headless 不导入 Qt。</p><p><strong>验收文件</strong>：<code>tests/test_behavior_plugin_integration.py</code>、<code>tests/test_plugin_sdk.py</code>、<code>tests/test_safe_capabilities.py</code>。</p><h3 id="n7-3-engine-api" tabindex="-1">N7.3 Engine API <a class="header-anchor" href="#n7-3-engine-api" aria-label="Permalink to &quot;N7.3 Engine API&quot;">​</a></h3><p><strong>Agent 要做</strong>：为调度器、渲染后端、资源类型、编辑器插件和编译器节点提供内部扩展点；明确版本迁移和普通项目不可依赖的边界；不把 Engine API 作为内容作者入口。</p><p><strong>完成条件</strong>：Engine 扩展可演进，普通项目 schema/Runtime 契约不因内部变化隐式改变。</p><p><strong>验收文件</strong>：<code>tests/test_plugin_package_boundaries.py</code>、<code>tests/test_behavior_descriptor.py</code>、<code>tests/test_editor_regression_contracts.py</code>。</p><h3 id="n7-4-分区包、依赖锁、加载与回滚" tabindex="-1">N7.4 分区包、依赖锁、加载与回滚 <a class="header-anchor" href="#n7-4-分区包、依赖锁、加载与回滚" aria-label="Permalink to &quot;N7.4 分区包、依赖锁、加载与回滚&quot;">​</a></h3><p><strong>Agent 要做</strong>：将 manifest、dependency lock、schema/migration、runtime/compiler、tool、assets/examples/presets 分区；加载、热重载、回滚和卸载是事务；Tool 按需加载，headless 不导入 Qt，Runtime 失败清理所有 owner/lease。</p><p><strong>完成条件</strong>：缺依赖、未知 capability、迁移失败和卸载异常都有结构化诊断，不污染文档或进程。</p><p><strong>验收文件</strong>：<code>tests/test_plugin_package_boundaries.py</code>、<code>tests/test_plugin_sdk.py</code>、<code>tests/test_behavior_plugin_integration.py</code>。</p><h3 id="n7-5-complexmapemitter-示范插件" tabindex="-1">N7.5 ComplexMapEmitter 示范插件 <a class="header-anchor" href="#n7-5-complexmapemitter-示范插件" aria-label="Permalink to &quot;N7.5 ComplexMapEmitter 示范插件&quot;">​</a></h3><p><strong>Agent 要做</strong>：实现真实 <code>ComplexMapEmitter</code>，暴露采样域、<code>z²+c</code> 函数、采样数、位置映射、颜色、Inspector、画布控制柄、预览和 debug snapshot；普通作者只调参数，高级作者可定位源码。</p><p><strong>完成条件</strong>：同一实例在 Content、Runtime、Tool 和 trace 中 identity 一致；源码扩展不要求普通作者进入 Engine API。</p><p><strong>验收文件</strong>：<code>tests/test_behavior_plugin_integration.py</code>、<code>tests/test_plugin_sdk.py</code>、<code>tests/test_editor_authoring_integration.py</code>；另附正式预览 native visual parity。</p><h2 id="_11-n8-renderer-pass-与-render-graph" tabindex="-1">11. N8：Renderer Pass 与 Render Graph <a class="header-anchor" href="#_11-n8-renderer-pass-与-render-graph" aria-label="Permalink to &quot;11. N8：Renderer Pass 与 Render Graph&quot;">​</a></h2><h3 id="n8-0-contract-后端中立-ir" tabindex="-1">N8.0 Contract：后端中立 IR <a class="header-anchor" href="#n8-0-contract-后端中立-ir" aria-label="Permalink to &quot;N8.0 Contract：后端中立 IR&quot;">​</a></h3><p><strong>Agent 要做</strong>：创建 <code>tests/test_render_graph.py</code>、<code>tests/test_renderer_pass_plugins.py</code>、<code>tests/test_render_graph_backend.py</code>、<code>tests/test_renderer_pass_editor.py</code>；冻结 attachment、格式/尺寸/sample、依赖、hazard、resource lease、fallback 和错误定位。</p><p><strong>完成条件</strong>：Contract 能拒绝环、未初始化读取、格式冲突、过期 lease 和未声明 capability。</p><p><strong>验收文件</strong>：上述四份 Contract。</p><h3 id="n8-1-passdescriptor-与编译器" tabindex="-1">N8.1 PassDescriptor 与编译器 <a class="header-anchor" href="#n8-1-passdescriptor-与编译器" aria-label="Permalink to &quot;N8.1 PassDescriptor 与编译器&quot;">​</a></h3><p><strong>Agent 要做</strong>：定义后端中立 <code>PassDescriptor</code>、输入/输出 attachment、load/store、参数 schema、合并和生命周期；编译期拒绝环、未初始化读取、格式冲突和 capability 不足；IR 不携带 ModernGL 对象。</p><p><strong>完成条件</strong>：Pass 顺序、资源依赖和失败路径可序列化、可诊断、可重放。</p><p><strong>验收文件</strong>：<code>tests/test_render_graph.py</code>、<code>tests/test_render_graph_backend.py</code>。</p><h3 id="n8-2-受限-passcontext-与资源-lease" tabindex="-1">N8.2 受限 PassContext 与资源 lease <a class="header-anchor" href="#n8-2-受限-passcontext-与资源-lease" aria-label="Permalink to &quot;N8.2 受限 PassContext 与资源 lease&quot;">​</a></h3><p><strong>Agent 要做</strong>：Runtime 插件只获得 scoped <code>PassContext</code>、<code>CommandEncoder</code> 和 resource lease；不能取得全局 GL state、持有过期资源或跨 owner 释放资源；热卸载必须结束 lease。</p><p><strong>完成条件</strong>：越权、跨帧资源和清理错误均显式失败，不靠静默替代。</p><p><strong>验收文件</strong>：<code>tests/test_renderer_pass_plugins.py</code>、<code>tests/test_render_graph.py</code>。</p><h3 id="n8-3-moderngl-backend-adapter" tabindex="-1">N8.3 ModernGL backend adapter <a class="header-anchor" href="#n8-3-moderngl-backend-adapter" aria-label="Permalink to &quot;N8.3 ModernGL backend adapter&quot;">​</a></h3><p><strong>Agent 要做</strong>：接入正式 ModernGL/GLFW 路径；adapter 将 IR 转成命令；缺能力只在 Descriptor 明确允许且语义可接受时 fallback，否则在编译/导出阶段报错；验证热重载和资源释放。</p><p><strong>完成条件</strong>：正式 surface 的输出与资源生命周期可追踪；Qt 预览不冒充 renderer 证据。</p><p><strong>验收文件</strong>：<code>tests/test_render_graph_backend.py</code>、<code>tests/test_renderer_pass_plugins.py</code>、<code>tests/test_background_data_driven_parity.py</code>；附 GLFW/ModernGL native 证据。</p><h3 id="n8-4-tool-runtime-parity" tabindex="-1">N8.4 Tool/Runtime parity <a class="header-anchor" href="#n8-4-tool-runtime-parity" aria-label="Permalink to &quot;N8.4 Tool/Runtime parity&quot;">​</a></h3><p><strong>Agent 要做</strong>：Tool 复用同一 Descriptor 提供 Inspector、控制柄和缩略预览；缺 Tool contribution 时退化为通用 Inspector；不得另建与正式 Render Graph 不同的效果实现。</p><p><strong>完成条件</strong>：编辑器参数、正式 renderer 和 debug snapshot 对同一实例保持一致。</p><p><strong>验收文件</strong>：<code>tests/test_renderer_pass_editor.py</code>、<code>tests/test_editor_authoring_integration.py</code>、<code>tests/test_render_graph_backend.py</code>。</p><h2 id="_12-n9-分层调试、确定性重放、性能与发布" tabindex="-1">12. N9：分层调试、确定性重放、性能与发布 <a class="header-anchor" href="#_12-n9-分层调试、确定性重放、性能与发布" aria-label="Permalink to &quot;12. N9：分层调试、确定性重放、性能与发布&quot;">​</a></h2><h3 id="n9-0-contract-trace-与-release-workload" tabindex="-1">N9.0 Contract：trace 与 release workload <a class="header-anchor" href="#n9-0-contract-trace-与-release-workload" aria-label="Permalink to &quot;N9.0 Contract：trace 与 release workload&quot;">​</a></h3><p><strong>Agent 要做</strong>：创建 <code>tests/test_editor_debugger.py</code>、<code>tests/test_replay_determinism.py</code>、<code>tests/test_runtime_profile.py</code>、<code>tests/test_editor_next_acceptance.py</code>；冻结 trace 协议版本、why-not 字段、replay identity 和 release workload。</p><p><strong>完成条件</strong>：Contract 能验证相同输入的 trace identity、未触发/取消/冲突的原因和预算字段。</p><p><strong>验收文件</strong>：上述四份 Contract。</p><h3 id="n9-1-统一-trace-与-why-not-调试器" tabindex="-1">N9.1 统一 trace 与 why-not 调试器 <a class="header-anchor" href="#n9-1-统一-trace-与-why-not-调试器" aria-label="Permalink to &quot;N9.1 统一 trace 与 why-not 调试器&quot;">​</a></h3><p><strong>Agent 要做</strong>：记录 author resource ID、runtime instance ID、owner/cancel token、State path、Clip/Reaction、事件因果、变量读写、生成数、batch 摘要、CPU/GPU、诊断和版本；调试器支持 pause/step/seek/reset/why-not，并能跳回资源、属性或行为节点。</p><p><strong>完成条件</strong>：能回答谁写/谁读、为何未触发、为何取消、为何冲突；overlay 不写作者文档。</p><p><strong>验收文件</strong>：<code>tests/test_editor_debugger.py</code>、<code>tests/test_editor_next_acceptance.py</code>、N4/N7 回归。</p><h3 id="n9-2-确定性-replay-seek" tabindex="-1">N9.2 确定性 replay/seek <a class="header-anchor" href="#n9-2-确定性-replay-seek" aria-label="Permalink to &quot;N9.2 确定性 replay/seek&quot;">​</a></h3><p><strong>Agent 要做</strong>：将 seed、外部输入、资源/插件版本、初始变量和副作用 policy 纳入 replay identity；seek 只能 reset+固定帧重放，若加入检查点必须证明逐帧等价并可回退；未记录的外部输入标记为不可复现。</p><p><strong>完成条件</strong>：同一 identity 的正常播放、reset、seek、热重载兼容路径 trace 逐帧一致，差异能定位到输入、版本、规则或资源。</p><p><strong>验收文件</strong>：<code>tests/test_replay_determinism.py</code>、<code>tests/test_variable_seek.py</code>、<code>tests/test_variable_hotreload.py</code>、<code>tests/test_preview_process.py</code>。</p><h3 id="n9-3-性能-profile-与预算" tabindex="-1">N9.3 性能 profile 与预算 <a class="header-anchor" href="#n9-3-性能-profile-与预算" aria-label="Permalink to &quot;N9.3 性能 profile 与预算&quot;">​</a></h3><p><strong>Agent 要做</strong>：在明确 Windows 目标硬件上测高密度 Pattern、batch lifecycle、Reaction、复杂 State/Timeline、ComplexMap、Render Graph 和长 seek；固定 workload、时间/内存/批量阈值、首次编译是否计入和报告格式；记录峰值，不用平均数掩盖超预算帧。</p><p><strong>完成条件</strong>：预算随 trace、导出 profile 和诊断记录；超预算阻止发布或给出明确降级，不静默删效果。</p><p><strong>验收文件</strong>：<code>tests/test_runtime_profile.py</code>、<code>tests/test_replay_determinism.py</code>、N3 batch tests、N8 backend tests；附目标硬件 Performance 报告。</p><h3 id="n9-4-最终作者工作流" tabindex="-1">N9.4 最终作者工作流 <a class="header-anchor" href="#n9-4-最终作者工作流" aria-label="Permalink to &quot;N9.4 最终作者工作流&quot;">​</a></h3><p><strong>Agent 要做</strong>：从模板完成短道中和两阶段 Boss，包含全灭后续、假 Boss 受击反击、死亡开花、背景切场、Runtime Behavior 和 Renderer Pass；保存、重开、Undo/Redo、热重载、seek、预览和导出均走正式路径。</p><p><strong>完成条件</strong>：顶层作者无需接触 Engine API；高级作者能从可视结构进入源码；所有实例和钩子可在调试器定位。</p><p><strong>验收文件</strong>：<code>tests/test_editor_next_acceptance.py</code>、<code>tests/test_editor_authoring_integration.py</code>、<code>tests/test_preview_process.py</code>、<code>tests/test_preview_editor_integration.py</code>、N4–N8 focused gate；附 native visual 和人工 workflow 报告。</p><h3 id="n9-5-发布门禁" tabindex="-1">N9.5 发布门禁 <a class="header-anchor" href="#n9-5-发布门禁" aria-label="Permalink to &quot;N9.5 发布门禁&quot;">​</a></h3><p><strong>Agent 要做</strong>：在同一 checkout 产生完整 tests、compileall、assets、diff、主游戏、Pattern/Stage preview、PySide6 窗口、正式 renderer、恢复、插件卸载和 profile 报告；分别给出 Structural/Runtime/Performance/Native visual/Usability 结论。</p><p><strong>完成条件</strong>：所有阶段停止条件关闭，focused gate、迁移、性能、原生窗口和人工可用性证据齐全后才允许发布；offscreen、<code>--help</code> 或单一“全绿”数字不能替代发布门禁。</p><p><strong>验收文件</strong>：N2–N9 全部 focused 文件、<code>tests/test_preview_process.py</code>、<code>tests/test_preview_editor_integration.py</code>、<code>tests/test_editor_authoring_integration.py</code>、<code>tests/test_editor_next_acceptance.py</code>，以及发布报告。</p><h2 id="_13-完成记录规则" tabindex="-1">13. 完成记录规则 <a class="header-anchor" href="#_13-完成记录规则" aria-label="Permalink to &quot;13. 完成记录规则&quot;">​</a></h2><ul><li>每个未完成任务只保留一个 <code>Evidence / Blocker</code> 段落。验证完成后原地更新该段落，再由主协调 Agent 把总表和任务状态从 <code>[ ]</code> 改为 <code>[x]</code>；不得追加交接日记、聊天记录或第二份 Todo。</li><li>Evidence 必须写完整命令、通过数量或报告路径、commit/工作树状态、Python/Qt/硬件环境，并分别标记 Structural、Runtime、Performance、Native visual、Usability。没有观察的类别写 <code>not run</code>，不能省略后暗示通过。</li><li>Contract、focused gate、迁移、性能、原生窗口或人工证据缺失时保持 <code>[ ]</code>；“代码已写完”“文件已存在”“offscreen 通过”都不构成缺失类别的证据。</li><li>禁止通过删测试、放宽断言、吞异常、<code>skip</code>/<code>xfail</code>、第二套预览、直接调用下游槽函数、伪造报告或写回运行时状态来“完成”任务。</li><li>若 ER 实现发现必须改变冻结产品语义，当前任务保持 <code>[ ]</code> 并请求维护者决定；不得在架构整改提交中静默修改契约。</li></ul>`,298)])])}const h=t(s,[["render",c]]);export{g as __pageData,h as default};
